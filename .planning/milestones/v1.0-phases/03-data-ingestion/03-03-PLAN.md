---
phase: 03-data-ingestion
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/swingrl/data/binance.py
  - tests/data/test_binance_ingestor.py
  - tests/data/fixtures/binance_klines_btcusdt.json
  - tests/data/fixtures/binance_archive_btcusdt_4h.csv
autonomous: true
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "BinanceIngestor.fetch() returns 4H OHLCV bars for BTCUSDT/ETHUSDT with USD-normalized volume"
    - "Rate limit monitoring reads X-MBX-USED-WEIGHT-1M header and sleeps when >80% of 1200 used"
    - "Backfill parses Binance archive CSVs correctly for both millisecond (pre-2025) and microsecond (2025+) timestamps"
    - "30-day stitch point validation confirms price deviation <0.5% between archive and API data"
    - "Volume normalization uses quote_asset_volume for USD, falls back to volume_base * close"
    - "CLI `python -m swingrl.data.binance` supports --backfill, --symbols, --days flags"
  artifacts:
    - path: "src/swingrl/data/binance.py"
      provides: "BinanceIngestor with fetch, backfill, rate limiting, and CLI"
      exports: ["BinanceIngestor"]
      min_lines: 150
    - path: "tests/data/test_binance_ingestor.py"
      provides: "Unit tests for Binance ingestor including backfill and rate limiting"
    - path: "tests/data/fixtures/binance_klines_btcusdt.json"
      provides: "Mock Binance.US klines API response"
    - path: "tests/data/fixtures/binance_archive_btcusdt_4h.csv"
      provides: "~100 row CSV mimicking data.binance.vision format with ms and us timestamps"
  key_links:
    - from: "src/swingrl/data/binance.py"
      to: "https://api.binance.us/api/v3/klines"
      via: "requests.get() with rate limit monitoring"
      pattern: "api\\.binance\\.us"
    - from: "src/swingrl/data/binance.py"
      to: "src/swingrl/data/base.py"
      via: "Inherits BaseIngestor"
      pattern: "class BinanceIngestor\\(BaseIngestor\\)"
    - from: "src/swingrl/data/binance.py"
      to: "src/swingrl/data/validation.py"
      via: "Uses DataValidator(source='crypto')"
      pattern: "DataValidator"
---

<objective>
Implement the Binance.US crypto data ingestor for BTC/ETH 4H bars with rate limit monitoring, historical backfill from public archives (2017-2019) stitched with API data (2019+), and volume normalization to USD.

Purpose: DATA-02 (live 4H ingestion) and DATA-03 (8+ year historical backfill) — crypto OHLCV data feeds the crypto RL environment.

Output: Working BinanceIngestor that writes crypto Parquet files to data/crypto/{SYMBOL}_4h.parquet.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-ingestion/03-CONTEXT.md
@.planning/phases/03-data-ingestion/03-RESEARCH.md
@.planning/phases/03-data-ingestion/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From src/swingrl/data/base.py:
```python
class BaseIngestor(abc.ABC):
    def __init__(self, config: SwingRLConfig) -> None: ...
    @abc.abstractmethod
    def fetch(self, symbol: str, since: str | None = None) -> pd.DataFrame: ...
    @abc.abstractmethod
    def validate(self, df: pd.DataFrame, symbol: str) -> tuple[pd.DataFrame, pd.DataFrame]: ...
    @abc.abstractmethod
    def store(self, df: pd.DataFrame, symbol: str) -> Path: ...
    def run(self, symbol: str, since: str | None = None) -> None: ...
```

From src/swingrl/data/validation.py:
```python
class DataValidator:
    def __init__(self, source: Literal["equity", "crypto", "fred"]) -> None: ...
    def validate_rows(self, df: pd.DataFrame, symbol: str) -> tuple[pd.DataFrame, pd.DataFrame]: ...
    def validate_batch(self, df: pd.DataFrame, symbol: str) -> pd.DataFrame: ...
```

From src/swingrl/data/parquet_store.py:
```python
class ParquetStore:
    def upsert(self, path: Path, new_df: pd.DataFrame) -> None: ...
    def read(self, path: Path) -> pd.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BinanceIngestor with 4H fetch, rate limiting, and klines parsing</name>
  <files>
    src/swingrl/data/binance.py
    tests/data/test_binance_ingestor.py
    tests/data/fixtures/binance_klines_btcusdt.json
  </files>
  <behavior>
    - test_fetch_returns_4h_ohlcv: mocked klines response -> fetch("BTCUSDT") returns DataFrame with open/high/low/close/volume columns, UTC timestamps at 4H intervals
    - test_volume_is_quote_asset: volume column uses quote_asset_volume (position [7] in klines response), not base volume
    - test_rate_limit_throttle: when response header X-MBX-USED-WEIGHT-1M > 960 (80% of 1200), time.sleep is called
    - test_rate_limit_no_throttle: when header shows weight < 960, no sleep
    - test_pagination_loop: when >1000 bars needed, fetch loops with advancing startTime until all bars collected
    - test_retry_on_api_error: 3 retries with exponential backoff (2s, 4s, 8s); raises DataError after exhaustion
    - test_store_writes_to_crypto_dir: store() writes to data/crypto/{SYMBOL}_4h.parquet
    - test_incremental_start_from_parquet: when Parquet exists, derives start from max(timestamp) + 1 bar (4H)
    - test_base_url_is_binance_us: requests go to api.binance.us, NOT api.binance.com
  </behavior>
  <action>
    1. Create tests/data/fixtures/binance_klines_btcusdt.json — 10 klines entries in the 12-element list format matching Binance API response. Timestamps in milliseconds. Include realistic BTC prices (~42000) and quote_asset_volume.

    2. Create tests/data/test_binance_ingestor.py — use `responses` library to mock requests.get calls to api.binance.us. Use monkeypatch for BINANCE_API_KEY and BINANCE_SECRET_KEY env vars. Each test docstring references DATA-02.

    3. Create src/swingrl/data/binance.py:
       - Module-level constants: BINANCE_US_BASE_URL = "https://api.binance.us", KLINES_ENDPOINT = "/api/v3/klines", WEIGHT_LIMIT = 1200, WEIGHT_THRESHOLD = 0.80, MAX_KLINES_PER_REQUEST = 1000, FOUR_HOURS_MS = 4 * 3600 * 1000
       - BinanceIngestor(BaseIngestor): constructor reads BINANCE_API_KEY/BINANCE_SECRET_KEY from os.environ (needed for authenticated endpoints, but klines is public — still pass for consistency)
       - _fetch_klines(symbol, interval, start_ms, end_ms): single paginated request, returns list[list]. Checks X-MBX-USED-WEIGHT-1M header, sleeps if > 80% of 1200
       - _parse_klines(raw): parse 12-element lists into DataFrame per RESEARCH.md Pattern 3. Columns: open/high/low/close/volume (volume = quote_asset_volume). UTC timestamps.
       - fetch(symbol, since): if since is None, start from 2019-01-01 (API data only, not archive). If "incremental", read max timestamp from Parquet. Paginate with _fetch_klines in a loop (startTime += MAX_KLINES * FOUR_HOURS_MS) until no more data. Concat all pages.
       - validate(df, symbol): delegates to DataValidator(source="crypto")
       - store(df, symbol): ParquetStore.upsert(data_dir / "crypto" / f"{symbol}_4h.parquet", df)
       - run_all(symbols, since): loop with try/except per symbol, return failed list
       - Retry: 3 attempts with time.sleep(2**attempt) on RequestException
       - if __name__ == "__main__" block with argparse: --symbols, --days, --backfill

    4. Update src/swingrl/data/__init__.py to export BinanceIngestor.
  </action>
  <verify>
    <automated>uv run pytest tests/data/test_binance_ingestor.py -v -x --tb=short -k "not archive and not stitch and not backfill"</automated>
  </verify>
  <done>
    - BinanceIngestor.fetch() returns 4H OHLCV with quote_asset_volume as volume
    - Rate limit monitoring works (throttles at 80% of 1200)
    - Pagination loops correctly for large date ranges
    - Retry logic handles API failures gracefully
    - All live-fetch unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add historical backfill with archive parsing, timestamp unit detection, stitch validation</name>
  <files>
    src/swingrl/data/binance.py
    tests/data/test_binance_ingestor.py
    tests/data/fixtures/binance_archive_btcusdt_4h.csv
  </files>
  <behavior>
    - test_archive_parse_milliseconds: CSV rows with ms timestamps (pre-2025) parsed correctly as UTC datetimes
    - test_archive_parse_microseconds: CSV rows with us timestamps (2025+) parsed correctly — threshold: >2_000_000_000_000 means microseconds
    - test_volume_normalization_quote_volume: when quote_asset_volume > 0, use it directly for volume
    - test_volume_normalization_fallback: when quote_asset_volume is 0 or missing, compute volume_base * close
    - test_stitch_validation_passes: archive and API overlap data within 0.5% price deviation -> no warning
    - test_stitch_validation_fails: archive and API overlap data with >0.5% deviation -> structlog warning with details
    - test_backfill_full_range: backfill(start_date="2017-01-01", end_date="present") stitches archive (2017-2019) + API (2019+) into single DataFrame
    - test_cli_backfill_flag: --backfill flag invokes backfill method
  </behavior>
  <action>
    1. Create tests/data/fixtures/binance_archive_btcusdt_4h.csv — ~100 rows mimicking data.binance.vision format. Include rows from 2018 (ms timestamps) AND rows from 2025 (us timestamps) to test both parsing paths. All 12 columns per Binance archive format. Some rows with zero quote_asset_volume to test fallback.

    2. Add tests to tests/data/test_binance_ingestor.py for all behaviors above. Mock file download for archive CSVs (use responses to mock the data.binance.vision URL). Use the fixture CSV for parsing tests.

    3. Add to src/swingrl/data/binance.py:
       - _parse_archive_csv(path_or_buffer): parse Binance archive CSV. Detect timestamp unit per row: if ts_raw > 2_000_000_000_000, unit="us" else unit="ms". Volume normalization: use quote_asset_volume (col 7) if > 0, else volume_base (col 5) * close (col 4). Return standard OHLCV DataFrame.
       - _validate_stitch(archive_df, api_df, overlap_days=30): compare close prices in the overlap window. For each matching timestamp, compute abs(archive_close - api_close) / api_close. Log discrepancies > 0.5% as warnings. Return bool (True if acceptable).
       - backfill(symbol, start_date, end_date):
         a) Download archive CSVs from data.binance.vision for date range up to stitch point (~2019-09-01 for Binance.US launch)
         b) Fetch API data from stitch point to end_date using fetch() pagination
         c) Fetch 30-day overlap from both sources
         d) Run _validate_stitch()
         e) Concatenate: archive data (up to stitch point) + API data (stitch point onward)
         f) Validate and store via validate() and store()
       - Update if __name__ == "__main__" to call backfill() when --backfill is set

    Note: The archive download URL pattern is: https://data.binance.vision/data/spot/monthly/klines/{SYMBOL}/4h/{SYMBOL}-4h-{YYYY}-{MM}.zip
    Each zip contains a single CSV file.
  </action>
  <verify>
    <automated>uv run pytest tests/data/test_binance_ingestor.py -v -x --tb=short</automated>
  </verify>
  <done>
    - Archive CSV parsing handles both ms and us timestamps correctly
    - Volume normalization uses quote_asset_volume with fallback to base * close
    - Stitch validation compares 30-day overlap and logs discrepancies >0.5%
    - Full backfill stitches archive + API data into continuous 4H series
    - All Binance ingestor tests pass (both live-fetch and backfill)
  </done>
</task>

</tasks>

<verification>
```bash
# All Binance tests pass
uv run pytest tests/data/test_binance_ingestor.py -v -x

# Import works
uv run python -c "from swingrl.data.binance import BinanceIngestor; print('OK')"

# Type checking
uv run mypy src/swingrl/data/binance.py --strict
```
</verification>

<success_criteria>
- BinanceIngestor inherits BaseIngestor and implements all abstract methods
- 4H klines fetched from api.binance.us (NOT api.binance.com)
- Rate limit monitoring pauses when X-MBX-USED-WEIGHT-1M exceeds 80% of 1200
- Archive parsing detects ms vs us timestamps automatically
- Stitch validation confirms <0.5% price deviation at the archive/API boundary
- CLI supports --backfill for full historical fetch
- All tests pass with mocked HTTP
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-ingestion/03-03-SUMMARY.md`
</output>
