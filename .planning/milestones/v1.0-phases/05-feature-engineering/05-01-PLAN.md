---
phase: 05-feature-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/swingrl/features/__init__.py
  - src/swingrl/features/technical.py
  - src/swingrl/features/schema.py
  - src/swingrl/config/schema.py
  - config/swingrl.yaml
  - config/swingrl.prod.yaml.example
  - tests/features/__init__.py
  - tests/features/conftest.py
  - tests/features/test_technical.py
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-10, FEAT-11]

must_haves:
  truths:
    - "9 price-action indicators compute correctly from OHLCV bars for any symbol"
    - "Weekly-derived features (trend direction, weekly RSI-14) compute from aggregated weekly bars"
    - "Derived features (log returns 1d/5d/20d, Bollinger width) compute for HMM input"
    - "DuckDB features_equity and features_crypto tables can be created via init_schema()"
    - "FeaturesConfig section exists in SwingRLConfig with normalization windows, HMM params, correlation threshold"
  artifacts:
    - path: "src/swingrl/features/technical.py"
      provides: "TechnicalIndicatorCalculator with compute_price_action(), compute_weekly_features(), compute_derived()"
      min_lines: 80
    - path: "src/swingrl/features/schema.py"
      provides: "DuckDB DDL for features_equity, features_crypto, fundamentals, hmm_state_history tables"
      contains: "CREATE TABLE IF NOT EXISTS features_equity"
    - path: "src/swingrl/config/schema.py"
      provides: "FeaturesConfig model added to SwingRLConfig"
      contains: "class FeaturesConfig"
    - path: "tests/features/conftest.py"
      provides: "Shared fixtures for feature tests (synthetic OHLCV, mock DuckDB)"
      min_lines: 30
  key_links:
    - from: "src/swingrl/features/technical.py"
      to: "stockstats.StockDataFrame"
      via: "retype() wrapper"
      pattern: "StockDataFrame\\.retype"
    - from: "src/swingrl/features/schema.py"
      to: "DatabaseManager"
      via: "init_feature_schema() using duckdb() context manager"
      pattern: "db\\.duckdb"
---

<objective>
Create the feature engineering foundation: config schema additions, DuckDB DDL for all Phase 5 tables, technical indicator computation (9 price-action + weekly-derived + HMM-input derived features), and test infrastructure.

Purpose: Establishes the core computation layer that all other feature plans depend on (indicators feed into normalization, assembler, and HMM). Also creates shared test fixtures used by all subsequent feature test files.
Output: FeaturesConfig in schema, 4 DuckDB table DDLs, TechnicalIndicatorCalculator class, test fixtures + test file
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-feature-engineering/05-CONTEXT.md
@.planning/phases/05-feature-engineering/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/swingrl/config/schema.py:
```python
class SwingRLConfig(BaseSettings):
    trading_mode: Literal["paper", "live"]
    equity: EquityConfig
    crypto: CryptoConfig
    capital: CapitalConfig
    paths: PathsConfig
    logging: LoggingConfig
    system: SystemConfig
    alerting: AlertingConfig
    # Plan 01 adds: features: FeaturesConfig

def load_config(path: Path | str = "config/swingrl.yaml") -> SwingRLConfig
```

From src/swingrl/data/db.py:
```python
class DatabaseManager:
    def duckdb(self) -> Generator[Any, None, None]: ...
    def sqlite(self) -> Generator[sqlite3.Connection, None, None]: ...
    def init_schema(self) -> None: ...
```

From tests/conftest.py (existing fixtures):
```python
@pytest.fixture
def equity_ohlcv() -> pd.DataFrame:  # 20 rows, seed=42
@pytest.fixture
def crypto_ohlcv() -> pd.DataFrame:  # 40 rows, seed=43
@pytest.fixture
def loaded_config(tmp_config) -> SwingRLConfig
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config schema + DuckDB DDL + test infrastructure</name>
  <files>
    src/swingrl/features/__init__.py,
    src/swingrl/features/schema.py,
    src/swingrl/config/schema.py,
    config/swingrl.yaml,
    config/swingrl.prod.yaml.example,
    tests/features/__init__.py,
    tests/features/conftest.py
  </files>
  <behavior>
    - Test: FeaturesConfig loads with default normalization windows (equity_zscore_window=252, crypto_zscore_window=360)
    - Test: FeaturesConfig loads HMM params (equity_hmm_window=1260, crypto_hmm_window=2000, hmm_n_iter=200, hmm_n_inits=5)
    - Test: FeaturesConfig loads correlation_threshold=0.85
    - Test: init_feature_schema() creates features_equity, features_crypto, fundamentals, hmm_state_history tables in DuckDB
    - Test: features_equity table accepts INSERT with (symbol TEXT, date DATE, + 15 feature columns DOUBLE)
    - Test: features_crypto table accepts INSERT with (symbol TEXT, datetime TIMESTAMP, + 13 feature columns DOUBLE)
  </behavior>
  <action>
    1. Create `src/swingrl/features/__init__.py` with `"""Feature engineering pipeline for SwingRL."""` and public exports.

    2. Create `FeaturesConfig` Pydantic model in `src/swingrl/config/schema.py`:
       - equity_zscore_window: int = 252
       - crypto_zscore_window: int = 360
       - equity_hmm_window: int = 1260 (5 years daily)
       - crypto_hmm_window: int = 2000 (4H bars ~1 year)
       - hmm_n_iter: int = 200
       - hmm_n_inits: int = 5
       - hmm_ridge: float = 1e-6
       - correlation_threshold: float = 0.85
       - zscore_epsilon: float = 1e-8
       Add `features: FeaturesConfig = Field(default_factory=FeaturesConfig)` to SwingRLConfig.

    3. Add `features:` section to config/swingrl.yaml and config/swingrl.prod.yaml.example with all fields and comments.

    4. Create `src/swingrl/features/schema.py` with `init_feature_schema(db: DatabaseManager)` function that creates 4 DuckDB tables:
       - features_equity: symbol TEXT, date DATE, price_sma50_ratio DOUBLE, price_sma200_ratio DOUBLE, rsi_14 DOUBLE, macd_line DOUBLE, macd_histogram DOUBLE, bb_position DOUBLE, atr_14_pct DOUBLE, volume_sma20_ratio DOUBLE, adx_14 DOUBLE, weekly_trend_dir DOUBLE, weekly_rsi_14 DOUBLE, pe_zscore DOUBLE, earnings_growth DOUBLE, debt_to_equity DOUBLE, dividend_yield DOUBLE, PRIMARY KEY (symbol, date)
       - features_crypto: symbol TEXT, datetime TIMESTAMP, price_sma50_ratio DOUBLE, price_sma200_ratio DOUBLE, rsi_14 DOUBLE, macd_line DOUBLE, macd_histogram DOUBLE, bb_position DOUBLE, atr_14_pct DOUBLE, volume_sma20_ratio DOUBLE, adx_14 DOUBLE, daily_trend_dir DOUBLE, daily_rsi_14 DOUBLE, four_h_rsi_14 DOUBLE, four_h_price_sma20_ratio DOUBLE, PRIMARY KEY (symbol, datetime)
       - fundamentals: symbol TEXT, date DATE, pe_ratio DOUBLE, earnings_growth DOUBLE, debt_to_equity DOUBLE, dividend_yield DOUBLE, sector TEXT, fetched_at TIMESTAMP, PRIMARY KEY (symbol, date)
       - hmm_state_history: environment TEXT, date DATE, p_bull DOUBLE, p_bear DOUBLE, log_likelihood DOUBLE, fitted_at TIMESTAMP, PRIMARY KEY (environment, date)
       All CREATE TABLE IF NOT EXISTS for idempotency.

    5. Create `tests/features/__init__.py` (empty).

    6. Create `tests/features/conftest.py` with shared fixtures:
       - `equity_ohlcv_250()`: 300-row equity OHLCV DataFrame (enough for 200-bar SMA warmup + 50 computed rows), seed=44, daily DatetimeIndex UTC
       - `crypto_ohlcv_400()`: 450-row crypto 4H OHLCV DataFrame (enough for 360-bar warmup + 90 computed rows), seed=45
       - `feature_config()`: loaded SwingRLConfig with features section
       - `duckdb_conn()`: In-memory DuckDB connection for integration tests (no DatabaseManager needed)
  </action>
  <verify>
    <automated>uv run pytest tests/features/ -x -v -k "config or schema or fixture"</automated>
  </verify>
  <done>FeaturesConfig in SwingRLConfig with all params, 4 DuckDB DDLs in schema.py, shared test fixtures created, config YAML updated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Technical indicator calculator (9 price-action + weekly + derived)</name>
  <files>
    src/swingrl/features/technical.py,
    tests/features/test_technical.py
  </files>
  <behavior>
    - Test: compute_price_action(equity_ohlcv_250) returns DataFrame with 9 columns: price_sma50_ratio, price_sma200_ratio, rsi_14, macd_line, macd_histogram, bb_position, atr_14_pct, volume_sma20_ratio, adx_14
    - Test: After 200-bar warmup, all 9 indicators have no NaN values
    - Test: price_sma50_ratio and price_sma200_ratio are dimensionless ratios close to 1.0 (not raw dollar values)
    - Test: RSI-14 values fall within [0, 100]
    - Test: bb_position can exceed [0, 1] range (breakout signal, NOT clipped)
    - Test: atr_14_pct values are small positive fractions (not dollar amounts)
    - Test: volume_sma20_ratio handles zero volume SMA gracefully (returns 1.0)
    - Test: compute_weekly_features(equity_ohlcv_250) returns DataFrame with weekly_trend_dir (binary 0/1) and weekly_rsi_14
    - Test: compute_crypto_multi_timeframe(crypto_ohlcv_400) returns daily_trend_dir, daily_rsi_14, four_h_rsi_14, four_h_price_sma20_ratio
    - Test: compute_derived(close_series) returns log_return_1d, log_return_5d, log_return_20d, bb_width columns
    - Test: stockstats does not mutate the original DataFrame (copy safety)
  </behavior>
  <action>
    Create `src/swingrl/features/technical.py` with class `TechnicalIndicatorCalculator`:

    1. `compute_price_action(ohlcv: pd.DataFrame) -> pd.DataFrame`:
       - Use stockstats StockDataFrame.retype(ohlcv.copy()) per RESEARCH.md Pattern 1
       - Compute all 9 indicators using verified column names: close_50_sma, close_200_sma, rsi_14, macd, macdh, boll_ub, boll_lb, atr_14, adx, volume_20_sma
       - Price/SMA ratios: close / sma (dimensionless)
       - BB position: (close - boll_lb) / (boll_ub - boll_lb), default 0.5 when band_width=0. Do NOT clip -- values outside [0,1] are valid breakout signals
       - ATR % of price: atr_14 / close
       - Volume ratio: volume / volume_20_sma, default 1.0 when sma=0

    2. `compute_weekly_features(daily_ohlcv: pd.DataFrame) -> pd.DataFrame`:
       - Resample daily OHLCV to weekly (Friday close)
       - Compute weekly SMA-10 on close, trend_dir = 1 if close > SMA-10 else 0
       - Compute weekly RSI-14 via stockstats on resampled data
       - Forward-fill back to daily index

    3. `compute_crypto_multi_timeframe(four_h_ohlcv: pd.DataFrame) -> pd.DataFrame`:
       - Aggregate 4H bars to daily for daily_trend_dir and daily_rsi_14
       - Compute 4H RSI-14 and 4H Price/SMA-20 ratio directly on 4H data
       - Return all 4 columns aligned to 4H index

    4. `compute_derived(close: pd.Series) -> pd.DataFrame`:
       - log_return_1d, log_return_5d, log_return_20d via np.log(close / close.shift(n))
       - bb_width from stockstats boll_ub - boll_lb
       - These are intermediate features for HMM input and correlation analysis, NOT part of observation vector

    Use `from __future__ import annotations` at top. Type hints on all signatures. structlog for logging.
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_technical.py -x -v</automated>
  </verify>
  <done>All 9 price-action indicators compute correctly from OHLCV, weekly features compute from resampled bars, derived features ready for HMM input, no NaN after warmup period</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/features/ -x -v` all green
- `uv run pytest tests/ -v` full suite still passes (no regressions)
- FeaturesConfig loads from swingrl.yaml without validation errors
- DuckDB tables can be created and queried
</verification>

<success_criteria>
- 9 technical indicators produce correct values (verified against known formulas)
- Weekly features derive from aggregated bars (not fetched separately)
- DuckDB DDL creates 4 tables (features_equity, features_crypto, fundamentals, hmm_state_history)
- FeaturesConfig exists in SwingRLConfig with all normalization/HMM/correlation params
- Shared test fixtures available for all subsequent feature test files
</success_criteria>

<output>
After completion, create `.planning/phases/05-feature-engineering/05-01-SUMMARY.md`
</output>
