---
phase: 10-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/swingrl/config/schema.py
  - config/swingrl.yaml
  - config/swingrl.prod.yaml.example
  - pyproject.toml
  - src/swingrl/utils/logging.py
  - src/swingrl/utils/retry.py
  - tests/utils/test_file_logging.py
  - tests/test_retry.py
autonomous: true
requirements: [HARD-02, HARD-05]

must_haves:
  truths:
    - "Config schema includes backup, shadow, sentiment, and security sections with validated defaults"
    - "tenacity, transformers, finnhub-python, matplotlib are installable as project dependencies"
    - "JSON logs write structured events to a file in the logs/ directory with rotation"
    - "Retry decorator retries failed API calls with exponential backoff and jitter"
  artifacts:
    - path: "src/swingrl/config/schema.py"
      provides: "BackupConfig, ShadowConfig, SentimentConfig, SecurityConfig sub-models on SwingRLConfig"
      contains: "class BackupConfig"
    - path: "src/swingrl/utils/retry.py"
      provides: "Tenacity-based retry decorator for external API calls"
      exports: ["swingrl_retry"]
    - path: "src/swingrl/utils/logging.py"
      provides: "File logging handler with RotatingFileHandler for JSON logs"
      contains: "RotatingFileHandler"
    - path: "tests/test_retry.py"
      provides: "Tests for retry decorator behavior"
    - path: "tests/utils/test_file_logging.py"
      provides: "Tests for file-based JSON logging"
  key_links:
    - from: "src/swingrl/utils/retry.py"
      to: "tenacity"
      via: "decorator wrapping tenacity.retry"
      pattern: "from tenacity import"
    - from: "src/swingrl/utils/logging.py"
      to: "logs/ directory"
      via: "RotatingFileHandler writing JSON to file"
      pattern: "RotatingFileHandler"
    - from: "src/swingrl/config/schema.py"
      to: "SwingRLConfig"
      via: "new sub-model fields on root config"
      pattern: "backup.*BackupConfig|shadow.*ShadowConfig|sentiment.*SentimentConfig"
---

<objective>
Extend SwingRL's foundation for Phase 10 production hardening: add config sub-models (BackupConfig, ShadowConfig, SentimentConfig, SecurityConfig), install new dependencies (tenacity, transformers, finnhub-python, matplotlib), implement retry decorator and file-based JSON logging.

Purpose: All subsequent Phase 10 plans depend on these config sections and cross-cutting concerns.
Output: Extended config schema, retry utility, file logging, new dependencies installed.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-hardening/10-CONTEXT.md
@.planning/phases/10-production-hardening/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/swingrl/config/schema.py:
```python
class SwingRLConfig(BaseSettings):
    trading_mode: Literal["paper", "live"]
    equity: EquityConfig
    crypto: CryptoConfig
    capital: CapitalConfig
    paths: PathsConfig
    logging: LoggingConfig
    features: FeaturesConfig
    environment: EnvironmentConfig
    system: SystemConfig
    alerting: AlertingConfig
    scheduler: SchedulerConfig
```

From src/swingrl/utils/logging.py:
```python
def configure_logging(json_logs: bool = False, log_level: str = "INFO") -> None
```

From src/swingrl/utils/exceptions.py:
```python
class SwingRLError(Exception): ...
class BrokerError(SwingRLError): ...
class DataError(SwingRLError): ...
class ConfigError(SwingRLError): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config schema extensions and dependency installation</name>
  <files>src/swingrl/config/schema.py, config/swingrl.yaml, config/swingrl.prod.yaml.example, pyproject.toml, tests/test_config.py</files>
  <behavior>
    - Test: BackupConfig validates sqlite_retention_days >= 1, duckdb_rotate defaults False
    - Test: ShadowConfig validates equity_eval_days >= 5, crypto_eval_cycles >= 10, mdd_tolerance_ratio > 1.0
    - Test: SentimentConfig validates enabled defaults False, max_headlines_per_asset >= 1
    - Test: SecurityConfig validates key_rotation_days >= 30
    - Test: SwingRLConfig loads with all new sections from YAML with defaults
    - Test: Invalid values (e.g., negative retention days) raise ValidationError
  </behavior>
  <action>
    1. Run `uv add tenacity finnhub-python matplotlib` to install new dependencies. Add `transformers` to optional deps group (heavy, only needed when sentiment.enabled=True): add to pyproject.toml under `[project.optional-dependencies]` as `sentiment = ["transformers>=4.40"]`.

    2. Add four new Pydantic sub-models to src/swingrl/config/schema.py (after SchedulerConfig):
       - BackupConfig: sqlite_retention_days (int, default=14, ge=1), duckdb_rotate (bool, default=False), backup_dir (str, default="backups/"), offsite_host (str, default=""), offsite_path (str, default="")
       - ShadowConfig: equity_eval_days (int, default=10, ge=5), crypto_eval_cycles (int, default=30, ge=10), auto_promote (bool, default=True), mdd_tolerance_ratio (float, default=1.2, gt=1.0)
       - SentimentConfig: enabled (bool, default=False), model_name (str, default="ProsusAI/finbert"), max_headlines_per_asset (int, default=10, ge=1), finnhub_api_key (str, default="")
       - SecurityConfig: key_rotation_days (int, default=90, ge=30), env_file_permissions (str, default="600")

    3. Add to SwingRLConfig root: backup (BackupConfig), shadow (ShadowConfig), sentiment (SentimentConfig), security (SecurityConfig).

    4. Update config/swingrl.yaml with new sections using dev defaults (commented examples).

    5. Update config/swingrl.prod.yaml.example with production values.

    6. Add tests to tests/test_config.py for all new config sections (validation passes/fails).
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -x -v -k "backup or shadow or sentiment or security"</automated>
  </verify>
  <done>All 4 config sub-models added to SwingRLConfig, YAML files updated, dependencies installed, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retry decorator and file-based JSON logging</name>
  <files>src/swingrl/utils/retry.py, src/swingrl/utils/logging.py, tests/test_retry.py, tests/utils/__init__.py, tests/utils/test_file_logging.py</files>
  <behavior>
    - Test: swingrl_retry decorator retries on ConnectionError up to max_attempts times
    - Test: swingrl_retry uses exponential backoff (verifiable via tenacity statistics)
    - Test: swingrl_retry reraises after exhausting retries
    - Test: swingrl_retry does NOT retry on non-retryable exceptions (e.g., ValueError)
    - Test: configure_logging with log_file path creates a RotatingFileHandler writing JSON
    - Test: Log events appear in the file as valid JSON lines
    - Test: RotatingFileHandler rotates at max_bytes threshold
  </behavior>
  <action>
    1. Create src/swingrl/utils/retry.py with a `swingrl_retry` function/decorator factory:
       - Uses tenacity.retry with stop_after_attempt(default=5), wait_exponential(multiplier=1, min=1, max=30), retry_if_exception_type((ConnectionError, TimeoutError, OSError))
       - Accepts overrides: max_attempts, min_wait, max_wait, retryable_exceptions
       - Logs each retry attempt via structlog before_sleep callback
       - Returns the tenacity decorator

    2. Extend configure_logging() in src/swingrl/utils/logging.py:
       - Add optional parameter: log_file (Path | None = None), max_bytes (int = 10_485_760, i.e. 10MB), backup_count (int = 5)
       - When log_file is provided, create a logging.handlers.RotatingFileHandler with the JSON formatter
       - Add the file handler alongside the existing stream handler
       - File handler always uses JSONRenderer regardless of json_logs flag

    3. Create tests/utils/__init__.py (empty package init if not exists).

    4. Create tests/test_retry.py with tests for retry behavior (mock a function that fails N times then succeeds, verify retry count, verify non-retryable exceptions pass through).

    5. Create tests/utils/test_file_logging.py with tests for file logging (write to tmp_path, verify JSON lines, verify rotation).
  </action>
  <verify>
    <automated>uv run pytest tests/test_retry.py tests/utils/test_file_logging.py -x -v</automated>
  </verify>
  <done>swingrl_retry decorator retries with backoff, file logging writes JSON to rotating file, all tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_config.py tests/test_retry.py tests/utils/test_file_logging.py -x -v` all pass
- `uv run python -c "from swingrl.config.schema import load_config; c = load_config(); print(c.backup, c.shadow, c.sentiment, c.security)"` prints config objects
- `uv run python -c "from swingrl.utils.retry import swingrl_retry; print(swingrl_retry)"` imports successfully
</verification>

<success_criteria>
- SwingRLConfig has backup, shadow, sentiment, security sub-models with validated defaults
- tenacity, finnhub-python, matplotlib installed; transformers in optional sentiment group
- swingrl_retry decorator retries with exponential backoff and jitter
- configure_logging() supports optional file output with JSON rotation
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-01-SUMMARY.md`
</output>
