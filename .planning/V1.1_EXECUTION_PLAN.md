# SwingRL v1.1 — Execution Plan & Tracker

> **Canonical entry point for the v1.1 "Operational Deployment" program of work.**
> Read this FIRST in any new session, then drill into the linked specs/plans.
> Created 2026-06-05 ET. This is the single source of truth for *what stage we're in*
> and *what has been completed*.

---

## How to use this doc

- This is the **high-level tracker + index**. Detail lives in the linked specs, plans,
  and handoffs — this doc does not duplicate it (DRY). When detail and this doc disagree,
  the linked detail wins for *content*; this doc wins for *status/sequence*.
- **Whenever a gate is passed**, update the stage's Status and append a dated line to the
  Changelog. This is the anti-amnesia mechanism — never lose progress across sessions.
- Source-of-truth links:
  - 19.1 training detail (Groups A–E): [`.planning/PHASE_19.1_HANDOFF.md`](PHASE_19.1_HANDOFF.md)
  - 19.1 history / incident record: [`.planning/PHASE_19.1_ARCHIVE.md`](PHASE_19.1_ARCHIVE.md)
  - 19.2 architecture design: [`docs/superpowers/specs/2026-05-19-phase-19.2-testing-foundation-design.md`](../docs/superpowers/specs/2026-05-19-phase-19.2-testing-foundation-design.md)
  - Per-stage specs: `docs/superpowers/specs/` (written just-in-time per stage)
  - Per-stage plans: written just-in-time via the `writing-plans` skill; linked per stage below

---

## Why this plan exists (situation as of 2026-06-05 ET)

Three facts forced a re-sequencing of the back half of v1.1:

1. **The work branch is 249 commits ahead of `main`, and `main` is a strict ancestor**
   (zero divergence — clean linear history). `main` predates the Postgres migration;
   everything since early April (Postgres migration, memory infra, CPS framework,
   `docs/training/` reference set, the 19.2 spec) lives only on
   `gsd/phase-19.1-memory-agent-infrastructure-and-training`. That unmerged delta is a
   standing integration risk.

2. **The 19.1 PR cannot merge.** Local tests pass (`975 passed / 391 skipped / 0 failed`)
   only because `DATABASE_URL` is unset and live-DB tests skip. On homelab CI, where
   `DATABASE_URL` points at the shared `pg16`, ~50 tests fail from inter-test state
   pollution — and the test approach has destroyed production data multiple times.

3. **Memory is empirically *hurting* training.** Across iter 3–4, control folds (no LLM
   advice) beat treatment folds by **2.7–5.1× CPS**. The Phase 1 prompt+reward refocus
   (handoff Group C) is empirically mandatory; deferring it has real cost.

The 19.2 spec (2026-05-19) correctly diagnoses #2 and designs a four-layer defence, but
sequences the empirically-mandatory training fix (#3) *last* (19.2.6), behind a 7-phase,
36-module repository refactor. **This plan re-sequences:** build the minimal safety floor,
ship the training value and drain the 249-commit delta, *then* do the full architectural
hardening — instead of blocking the urgent fix behind the large refactor.

---

## Status legend

`☐ not started` · `📝 spec written` · `✅ spec approved` · `🗺 plan written` ·
`✅ plan approved` · `🔨 in progress` · `🧪 CI green` · `🚢 merged` · `✔ done`

---

## Branch strategy

- **Old convention `gsd/…` is retired** (GSD tooling no longer in use). The existing
  `gsd/phase-19.1-memory-agent-infrastructure-and-training` branch keeps its name purely
  for historical reference and serves as the **one-time consolidation vehicle**.
- **New convention: `swingrl/<N>-<slug>`** (e.g. `swingrl/19.1-training-refocus`,
  `swingrl/19.2.0-test-foundation`).
- **Consolidation flow:** finish Stage 1 *on the existing `gsd/` branch* → homelab CI green
  → open one consolidation PR → **`--no-ff` merge to `main` preserving all 249 commits**
  (retains Postgres-migration and iter 0–4 recovery forensics) → retire the branch.
- **From Stage 2 onward:** every phase gets a small, well-named `swingrl/<N>-<slug>` branch
  cut from the freshly-updated `main`. No more mega-branch.

---

## Gate model

Every stage passes these gates in order. Per `CLAUDE.md`, nothing is written without
G1/G2, and nothing merges without G3/G4.

| Gate | When | What happens |
|---|---|---|
| 🚩 **G1 — Spec approval** | after the stage spec is written | user reviews & approves the spec before any planning |
| 🚩 **G2 — Plan approval** | after `writing-plans` produces the plan | user reviews & approves the plan before any code |
| 🚩 **G3 — CI green** | after implementation | homelab `scripts/ci-homelab.sh` must pass before a PR is opened |
| 🚩 **G4 — Merge** | after PR opened | user merges (branch protection / self-approval) |

**Special gates (additive):**
- 🛑 **Backup gate** — any destructive production-DB operation (handoff Group A iter-5 wipe,
  Group D consolidation deletes) requires plan-mode approval **and a verified backup**
  immediately before execution.
- 🕐 **Market-hours gate** — execution/risk code (Stage 3.4) does **not** merge during US
  market hours, and requires paper-trading verification before merge.

---

## Stages at a glance

| Stage | Goal | Branch | Spec | Plan | Status |
|---|---|---|---|---|---|
| **0** | Planning docs corrected; master tracker live | `gsd/phase-19.1-…` | this doc | inline | ✔ done (merged with Stage 1) |
| **1** | Minimal safety floor → green CI → consolidation merge | `gsd/phase-19.1-…` → `main` | [stage1 spec](../docs/superpowers/specs/2026-06-05-stage1-safety-floor-design.md) | [stage1 plan](../docs/superpowers/plans/2026-06-05-stage1-safety-floor.md) | ✔ **done** — merged to `main` via PR #17 (2026-06-10); `gsd/` branch retired |
| **2** | Training refocus (Groups A–E) — reverse the 2.7–5.1× harm | `swingrl/19.1-training-refocus` | [stage2 spec](../docs/superpowers/specs/2026-06-11-stage2-training-refocus-design.md) | _TBD_ | ✅ spec approved (G1) |
| **3** | Architecture hardening (19.2.0-rest → 19.2.5) | `swingrl/19.2.x-…` (per sub-phase) | _TBD per sub-phase_ | _TBD_ | ☐ not started |
| **4** | Deferred bugs (Group F/G) + milestone closeout | `swingrl/19.x-closeout` | _TBD_ | _TBD_ | ☐ not started |

---

## Stage details

### Stage 0 — Planning docs + master tracker  🔨

**Goal:** Maps reflect reality so no session works from stale state.

**Deliverables**
- [x] Create this master tracker (`.planning/V1.1_EXECUTION_PLAN.md`)
- [x] Correct `.planning/ROADMAP.md` ("19.1 done / Phase 20 next" → reopened 19.1 + inserted 19.2)
- [x] Correct `.planning/PROJECT.md` (Postgres on branch not main; current milestone state)
- [x] Update `CLAUDE.md` git-workflow branch convention `gsd/phase-{N}-{slug}` → `swingrl/<N>-<slug>`

**Gates:** G1/G2 satisfied by the brainstorm + this approval; no CI/merge (docs ride along with Stage 1's consolidation merge). Awaiting user review of the four files.
**Acceptance:** A fresh session reading `.planning/` understands the true state in one pass.

---

### Stage 1 — Minimal safety floor → green CI → consolidation merge  ✔ (done — merged via PR #17, 2026-06-10)

**Goal:** Make tests safe and isolated, turn homelab CI green, and drain the 249-commit
delta to `main`. This is the safety-critical *subset* of 19.2.0 only — not the full refactor.

**Spec:** [`2026-06-05-stage1-safety-floor-design.md`](../docs/superpowers/specs/2026-06-05-stage1-safety-floor-design.md)
— approach approved in principle: **wipe-after-each-test** (TRUNCATE) instead of SAVEPOINT
rollback (a chunk of tests use raw autocommit+DDL connections that rollback can't fix), guarded
by a **3-layer model** (Postgres connection-binding + hardened pre-suite guard + pre-wipe
`_test` re-check). Separate `pg-test` server + rollback fixture **deferred to Stage 3.0**.

**Deliverables** — _(implemented; approach LOCKED to wipe-after-each-test, see [stage1 spec](../docs/superpowers/specs/2026-06-05-stage1-safety-floor-design.md))_
- [x] `tests/db_guard.py` — shared resolve(env→config)+classify helpers (the software checks atop Postgres connection-binding)
- [x] Hardened `tests/conftest.py::pytest_configure` to resolve env **and** config fallback (Check #1)
- [x] `tests/fixtures/db_cleanup.py` — autouse wipe-after-each-test fixture + pre-wipe `_test` re-check (Check #2)
- [x] DB-gated isolation regression test (`tests/test_db_isolation.py`)
- [x] Removed the six dead commented-out per-module TRUNCATE blocks (DRY)
- [x] **Follow-up fixes to reach green CI** (first CI run exposed 14 failures in 3 clusters):
  Cluster B (mine) — leaky `return conn` fixtures (`trade_db`, `pg_conn`) → `yield`+close so locks
  release before the wipe; Cluster C (pre-existing) — `len(memories or [])` guard at
  `consolidate.py:1436`; Cluster A (pre-existing) — `init_postgres_schema` in `test_validation`
  (was leaving a stale 22-col `backtest_results`) + data-present skip-guards on the live-data
  reporting tests.
- [x] 🚩 **G3: homelab `ci-homelab.sh` GREEN** — `1377 passed / 7 skipped / 0 failed`, all 6 stages,
  `=== CI PASSED ===` (run `bksv8snpk`, 2026-06-07; branch HEAD `3840749`)
- [ ] _(deferred to Stage 3.0)_ separate `pg-test` server + `SAVEPOINT` rollback fixture; CI keeps creating a fresh `swingrl_test` on `pg16` and the wipe fixture handles intra-run isolation
- [ ] _(deferred to Stage 4)_ F1 turbulence-column bug

**Gates:** G1 ✅ spec → G2 ✅ plan → G3 ✅ (homelab CI green, 0 failures) → **G4 ✅ merged to `main` via PR #17 admin-bypass merge commit (`a380030`, 2026-06-10), `gsd/` branch retired**.
**Acceptance:** ✅ met — homelab `ci-homelab.sh` green; `main` now contains Postgres + memory infra + CPS + docs + safety floor; the consolidation branch is merged and retired.

---

### Stage 2 — Training refocus (handoff Groups A–E)  ✅ G1 spec approved

**Goal:** Reverse the empirically-confirmed memory harm and bank the research value.
Detail: [`PHASE_19.1_HANDOFF.md`](PHASE_19.1_HANDOFF.md) Groups A–E.
**Spec:** [`2026-06-11-stage2-training-refocus-design.md`](../docs/superpowers/specs/2026-06-11-stage2-training-refocus-design.md)
— approved 2026-06-11 (G1). Key amendments to the handoff: **C4 base-weight rebalance DROPPED**
(control wins on current weights; keeps Group E a one-variable experiment); CPS v1 = single
objective via a **three-layer metric hierarchy** (North Star / epoch leading-indicators incl.
new trade-activity rate / per-advice attribution); new deterministic **`cps_diagnosis.py`**
labels *why* CPS is low (trade_shy / poor_selection / single_disaster / churning) so advice
matches cause. A/B/D/E execute as a gated runbook (spec §8).

**Deliverables (map to handoff groups)**
- [ ] **Group A** — wipe iter-5 artifacts from pg16/disk  🛑 *backup gate*
- [ ] **Group B** — rebuild the 3 containers so committed fixes (autocommit, deploy path, env-prefix) actually run
- [ ] **Group C** — prompt + reward refocus (the core fix) with C0 baseline & C6 post-change docs
- [ ] **Group D** — iteration-by-iteration pattern regeneration with QA gates  🛑 *backup gate on D1 deletes*
- [ ] **Group E** — fresh iter-5 re-run; capture treatment-vs-control CPS (the proof point)

**Gates:** G1 spec → G2 plan → (🛑 backup gates inside A/D) → 🚩 G3 CI green → 🚩 G4 merge.
**Acceptance:** Group E's treatment-vs-control CPS ratio collapses toward 1.0 (or inverts);
19.1 refocus merged to `main`.
**Branch:** `swingrl/19.1-training-refocus`.

> **Known overlap:** Group C edits memory files that Stage 3.4 later refactors. C touches
> *prompt text + reward weights*; 3.4 touches the *DB I/O boundary* — mostly different parts
> of the same files, low conflict. Accepted cost for shipping value first.

---

### Stage 3 — Architecture hardening (full 19.2)  ☐

**Goal:** Complete the four-layer defence and the repository refactor.
Design source: the 19.2 spec. Each sub-phase = its own thin spec + plan + `swingrl/19.2.x-…`
branch, each ending green on `pg-test`, each merged before the next.

| Sub-phase | Scope | Notes / gates |
|---|---|---|
| **3.0** | Remainder of 19.2.0: `ports/` package, `HaltRepository` template, ADR-0001 + ADR-0002, CLAUDE.md no-direct-DB rule, README pointer | the parts of 19.2.0 not needed for the Stage 1 floor |
| **3.1** | 19.2.1 — low-effort modules | pattern muscle |
| **3.2** | 19.2.2 — medium modules (6 ingestors, features, reporting, shadow, dashboard) | benchmark `training/data_loader` for perf regression; heavy `training-data-capture.md` rewrite |
| **3.3** | 19.2.3 — execution + risk modules | 🕐 strict TDD, no market-hours merge, paper-trading verification |
| **3.4** | 19.2.4 — memory subsystem refactor | carries Stage 2 prompt/reward changes forward; fixes `consolidate.py:1436` |
| **3.5** | 19.2.5 — server-side hardening (`swingrl_admin`, privilege revoke, `pg_hba.conf`) | 🕐 maintenance window, tested rollback; the one Stage-3 piece **not** to skip (genuine safety) |

**Note:** the spec's 19.2.6 ("19.1 reintegration") effectively disappears — 19.1 already
merged in Stage 2 — so it collapses into verification + roadmap closeout (Stage 4).
**Gates:** per sub-phase: G1 → G2 → G3 → G4.

---

### Stage 4 — Deferred bugs + milestone closeout  ☐

**Goal:** Clear tracked debt and close v1.1's training/testing arc.

**Deliverables**
- [ ] **F1** turbulence-column bug (if not already done in Stage 1/3.4) — **must precede Phase 20 live trading**
- [ ] **F2** SAC epoch-cadence root cause (Task C fixed the OOM symptom, not the cause)
- [ ] **F3** `llm_audit_log.iteration_number` NULL backfill (801 rows)
- [ ] **F4** full test suite against live Postgres before production
- [ ] **G1 (handoff)** decision: re-run `select_best_per_algo_env` across iter 0–5 vs accept iter-5 output
- [ ] Roadmap closeout; tag/release per `CLAUDE.md` milestone workflow

**Gates:** G1 → G2 → G3 → G4 as applicable.

---

## Open decisions & risks

- **F1 turbulence bug blocks live trading** (`execution/pipeline.py:537` queries a
  non-existent column; silent 0.0 fallback). Not blocking training; must be fixed before
  Phase 20. Can ride along Stage 3.4 if `pipeline.py` is already open.
- **Final reward weights** (Group C4) — unified `{profit, sharpe, drawdown, turnover}` plus
  a `CHRONIC_FAILURE_WEIGHTS` variant; exact values confirmed at Stage 2 spec time.
- **L3 ROI gut-check** — Stages 3.1–3.4 (the 36-module repository refactor) are the single
  biggest investment and deliver test-speed/maintainability, *not* safety (safety is won at
  L1+L4 in Stage 1 and L2 in Stage 3.5). Natural timebox/stop points: after Stage 1 (safe +
  unblocked) and after Stage 2 (value banked).
- **API keys** shared in chat 2026-03-24 must still be rotated (per project memory).

---

## Changelog

_Append-only. Date (ET) · what completed · gate passed._

- **2026-06-05** — Master execution plan drafted; staged re-sequencing approved
  (minimal floor → training value → hardening). Branch convention set to `swingrl/<N>-<slug>`;
  consolidation = `--no-ff` preserve-history.
- **2026-06-05** — Stage 0 ✔ done: master tracker + ROADMAP/PROJECT corrections + CLAUDE.md
  branch-convention update written and user-approved (G1/G2). Committed `cef6b18`.
- **2026-06-05** — Stage 1 spec written + committed (read-only test-architecture investigation
  done first). User approved the **wipe-after-each-test + 3-layer guard** approach in plain-English
  terms; separate `pg-test` server deferred to Stage 3.0. **⏸ Paused at G1** (session limit).
- **2026-06-06** — Stage 1 implemented (G2 plan approved → subagent-driven TDD): `tests/db_guard.py`
  helpers, hardened `pytest_configure`, autouse `db_cleanup.py` wipe fixture + pre-wipe re-check,
  DB-gated isolation test, six dead TRUNCATE blocks removed. Deliverables checklist synced to the
  locked wipe approach (pg-test/SAVEPOINT remain deferred to Stage 3.0). Local suite: 990 passed /
  394 skipped / 0 failed (DB tests skip with no DATABASE_URL). **⏸ Pending G3 homelab CI before
  consolidation merge.**
- **2026-06-07** — Stage 1 **G3 PASSED** 🧪. First CI run surfaced 14 failures in 3 clusters; all
  fixed (commits `138607c`/`31dac72`/`3840749`): **B** (mine) leaky `return conn` fixtures →
  `yield`+close; **C** (pre-existing) `len(memories or [])` guard at `consolidate.py:1436`;
  **A** (pre-existing) `init_postgres_schema` in `test_validation` + data-present skip-guards on
  live-data reporting tests. Re-run green: **1377 passed / 7 skipped / 0 failed**, `=== CI PASSED ===`
  (run `bksv8snpk`; branch HEAD `3840749`, pushed). **➡ Awaiting G4: `--no-ff` consolidation merge.**
- **2026-06-09** — G4 prep: opened **PR #17** (gsd → main, 271 commits). Surfaced a *separate*
  blocker — the GitHub Actions required check **`Tests (coverage >= 85%)` is RED** at **62.99%**:
  GHA runs pytest with **no Postgres DB**, so 394 DB tests skip and DB code is uncovered. **Pre-existing
  (since the April Postgres migration), not a Stage 1 regression**; 0 test failures. Filed as **issue
  #18** (add a Postgres service to `ci.yml`). Decision: **admin-bypass merge** PR #17 (branch
  protection `enforce_admins=false`); homelab CI is green so the code is sound.
- **2026-06-10** — Stage 1 **G4 PASSED** 🚢. PR #17 merged to `main` via admin-bypass **merge commit**
  (`a380030`, preserve-history — NOT squash). GHA coverage check left RED per issue #18 (no Postgres
  in GHA → DB tests skip; pre-existing, not a regression). `gsd/phase-19.1-…` retired (GitHub
  auto-deleted the head branch on merge; stale local tracking ref pruned). `main` now contains
  Postgres + memory infra + CPS + `docs/training/` + the Stage 1 safety floor; in sync with
  `origin/main`. **Stage 1 ✔ done (G0–G4 all green). ➡ Stage 2 (training refocus, Groups A–E) next.**
- **2026-06-11** — Stage 2 **G1 PASSED** ✅. Branch `swingrl/19.1-training-refocus` cut from `main`;
  spec written + approved (`docs/superpowers/specs/2026-06-11-stage2-training-refocus-design.md`).
  Decisions: C4 dropped (control wins on current base weights; preserve one-variable Group E);
  CPS v1 single objective + 3-layer metric hierarchy + new trade-activity leading indicator;
  new deterministic `cps_diagnosis.py` (cause-labeled advice); A/B/D/E as gated runbook.
  Also retired stale `gsd/` remote refs (prune). **➡ Next: G2 — implementation plan.**

---

## ▶ RESUME HERE (next session)

**State:** **Stage 1 ✔ DONE — all gates G1→G4 passed.** PR #17 merged to `main` via admin-bypass
**merge commit** (`a380030`, 2026-06-10; preserve-history, not squash). `gsd/phase-19.1-…` branch
retired. `main` now holds Postgres + memory infra + CPS framework + `docs/training/` + the Stage 1
safety floor. Working tree is on `main`, in sync with `origin/main` (clean).

⚠️ The GHA required check **`Tests (coverage >= 85%)`** is still RED (~63% — GHA has no Postgres, so
DB tests skip and DB code is uncovered). **Pre-existing, not a regression**; tracked in **issue #18**
(fix = add a Postgres service to `.github/workflows/ci.yml`). Not blocking — homelab CI is the real
gate and it is green. Do NOT "fix tests" for this.

**Next action — Stage 2 G2 (implementation plan):** Stage 2 **G1 spec is approved**
([`2026-06-11-stage2-training-refocus-design.md`](../docs/superpowers/specs/2026-06-11-stage2-training-refocus-design.md))
on branch `swingrl/19.1-training-refocus`. Write the implementation plan (writing-plans skill) for
user G2 review — spec §11 lists the four open items the plan must resolve against iter 0–4 data
(diagnosis thresholds, trade-rate baseline, payload shapes, orchestrator recompute-vs-read).
Execution sequence after G2: A (wipe iter 5 🛑) → B (rebuild 3 containers) → C (code via TDD → G3
CI → G4 PR) → D (per-iter pattern regen w/ QA 🛑 on D1) → E (fresh iter-5 run; CPS proof point).
Detail: [`PHASE_19.1_HANDOFF.md`](PHASE_19.1_HANDOFF.md) Groups A–E (as amended by spec §2) and Stage 2 above.

🛑 **Backup gates apply inside Stage 2.** Group A wipes ~850k iter-5 rows + iter-5 model files; Group D
deletes iter 0–4 consolidations. Each destructive DELETE needs its own plan-mode approval **and a
verified backup** taken immediately before execution — never run them as part of bulk approval.

**Do NOT** re-run Stage 1 work or re-litigate the wipe approach — it's done, green, and merged.
