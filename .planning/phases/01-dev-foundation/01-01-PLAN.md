---
phase: 01-dev-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/swingrl/__init__.py
  - src/swingrl/py.typed
  - src/swingrl/data/__init__.py
  - src/swingrl/envs/__init__.py
  - src/swingrl/agents/__init__.py
  - src/swingrl/training/__init__.py
  - src/swingrl/execution/__init__.py
  - src/swingrl/monitoring/__init__.py
  - src/swingrl/config/__init__.py
  - src/swingrl/utils/__init__.py
  - tests/conftest.py
  - tests/test_smoke.py
  - config/.gitkeep
  - data/.gitkeep
  - db/.gitkeep
  - models/.gitkeep
  - scripts/.gitkeep
  - status/.gitkeep
  - logs/.gitkeep
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04

must_haves:
  truths:
    - "uv sync completes without resolver errors on M1 Mac"
    - "import swingrl succeeds in the venv"
    - "import torch succeeds and torch.backends.mps.is_available() returns True on M1 Mac"
    - "All 8 canonical directories exist at repo root (src/, config/, data/, db/, models/, tests/, scripts/, status/)"
    - "All 8 swingrl subpackages have __init__.py files"
    - "pytest tests/test_smoke.py passes with all tests green"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, all dependencies, tool configs (ruff, black, mypy, bandit, pytest)"
      contains: "swingrl"
    - path: "uv.lock"
      provides: "Reproducible multi-platform dependency lockfile"
    - path: "src/swingrl/__init__.py"
      provides: "Package root"
    - path: "src/swingrl/py.typed"
      provides: "PEP 561 type annotation marker"
    - path: "tests/test_smoke.py"
      provides: "Import smoke tests + directory structure validation"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock generates lockfile from project deps"
      pattern: "uv\\.lock"
    - from: "tests/test_smoke.py"
      to: "src/swingrl/__init__.py"
      via: "import swingrl in test"
      pattern: "import swingrl"
---

<objective>
Create the SwingRL repository scaffold with all directories, pyproject.toml with full dependency set, uv.lock, and smoke tests that validate the environment.

Purpose: Establish the foundational project structure and dependency resolution that all subsequent plans build on. Catching version conflicts (especially FinRL + SB3 + torch) now prevents cascading failures later.

Output: Working Python package with all deps resolved, importable swingrl package, passing smoke tests.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dev-foundation/01-CONTEXT.md
@.planning/phases/01-dev-foundation/01-RESEARCH.md
@.planning/phases/01-dev-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repository scaffold and pyproject.toml with full dependency resolution</name>
  <files>
    pyproject.toml, .gitignore, .env.example,
    src/swingrl/__init__.py, src/swingrl/py.typed,
    src/swingrl/data/__init__.py, src/swingrl/envs/__init__.py,
    src/swingrl/agents/__init__.py, src/swingrl/training/__init__.py,
    src/swingrl/execution/__init__.py, src/swingrl/monitoring/__init__.py,
    src/swingrl/config/__init__.py, src/swingrl/utils/__init__.py,
    config/.gitkeep, data/.gitkeep, db/.gitkeep, models/.gitkeep,
    scripts/.gitkeep, status/.gitkeep, logs/.gitkeep
  </files>
  <action>
1. Create all top-level directories with .gitkeep files: config/, data/, db/, models/, scripts/, status/, logs/

2. Create src/swingrl/ package with __init__.py (containing `__version__ = "0.1.0"`) and py.typed marker (empty file). Create all 8 subpackages (data, envs, agents, training, execution, monitoring, config, utils) each with an empty __init__.py.

3. Create .gitignore covering: __pycache__, .venv/, *.pyc, .env (but NOT .env.example), data/* (but not data/.gitkeep), db/* (but not db/.gitkeep), models/active/*, models/shadow/*, models/archive/*, logs/* (but not .gitkeep files), .DS_Store, *.egg-info, dist/, build/, .mypy_cache/, .ruff_cache/, .pytest_cache/

4. Create .env.example with placeholder keys: ALPACA_API_KEY, ALPACA_SECRET_KEY, BINANCE_API_KEY, BINANCE_SECRET_KEY, DISCORD_WEBHOOK_URL, FRED_API_KEY — all set to empty strings with comments.

5. Create pyproject.toml using the EXACT template from 01-RESEARCH.md "pyproject.toml Full Template" section. This includes:
   - build-system: hatchling
   - name: swingrl, version: 0.1.0, requires-python: ">=3.11,<3.12"
   - All dependencies: torch>=2.2,<2.4, torchvision>=0.17,<0.20, stable-baselines3[extra]>=2.0,<3, gymnasium>=0.29.1, finrl==0.3.7, pandas>=2.0,<3, numpy>=1.26,<2, pandas-ta>=0.3.14b, hmmlearn>=0.3, alpaca-py>=0.20, pydantic>=2.5, pyyaml>=6.0
   - [tool.uv.sources] for platform-specific torch (CPU on Linux, standard on macOS)
   - [[tool.uv.index]] for pytorch-cpu with explicit=true
   - [dependency-groups] dev with pytest, pre-commit, ruff, black, mypy, bandit[toml], detect-secrets, pandas-stubs, types-requests
   - [tool.hatch.build.targets.wheel] packages = ["src/swingrl"]
   - Tool configs for pytest, black (line-length=100), ruff (line-length=100, select E/W/F/I/B/C4/UP/N, ignore E501/B008/N803/N806), mypy (disallow_untyped_defs=true, ignore_missing_imports for finrl/pandas_ta/hmmlearn/alpaca), bandit (exclude_dirs tests/.venv, assert_used skips for test files)

6. Run `uv sync --group dev` to resolve all dependencies and generate uv.lock. If FinRL 0.3.7 causes resolver conflicts:
   - First try: Add `finrl = { git = "https://github.com/AI4Finance-Foundation/FinRL.git" }` to [tool.uv.sources]
   - If still failing: Pin FinRL to a specific git commit from master
   - Document the resolution chosen in the commit message

7. Verify: `uv run python -c "import torch; print(torch.__version__); print('MPS:', torch.backends.mps.is_available())"` — torch imports and MPS is True on Mac.

8. Verify: `uv run python -c "import swingrl; print(swingrl.__version__)"` prints 0.1.0
  </action>
  <verify>
    <automated>uv sync --locked && uv run python -c "import swingrl; import torch; print('OK')"</automated>
  </verify>
  <done>
    - pyproject.toml exists with all dependencies and tool configs
    - uv.lock exists and is committed
    - All 8 top-level directories exist with .gitkeep
    - src/swingrl/ importable with all 8 subpackages having __init__.py
    - py.typed marker exists
    - torch imports successfully with MPS available on Mac
    - No resolver errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create smoke tests and conftest</name>
  <files>tests/test_smoke.py, tests/conftest.py, tests/data/.gitkeep, tests/envs/.gitkeep, tests/agents/.gitkeep, tests/training/.gitkeep, tests/execution/.gitkeep, tests/monitoring/.gitkeep, tests/config/.gitkeep, tests/utils/.gitkeep</files>
  <action>
1. Create test subdirectories mirroring src/swingrl/ subpackages: tests/data/, tests/envs/, tests/agents/, tests/training/, tests/execution/, tests/monitoring/, tests/config/, tests/utils/ — each with a .gitkeep file.

2. Create tests/conftest.py with:
   - A `repo_root` fixture returning `Path(__file__).parent.parent` (session scope)
   - A brief docstring: "SwingRL shared test fixtures."
   - Type annotations on all fixtures

3. Create tests/test_smoke.py using the EXACT test code from 01-RESEARCH.md "tests/test_smoke.py (Phase 1 Baseline)" section. This includes:
   - test_python_version: asserts sys.version_info[:2] == (3, 11)
   - test_torch_importable: imports torch, asserts version exists
   - test_torch_mps_available_on_mac: skips on non-Darwin, asserts MPS available
   - test_core_imports: imports stable_baselines3, gymnasium, pandas, numpy, pandas_ta, hmmlearn, pydantic
   - test_swingrl_package_importable: imports swingrl
   - test_py_typed_marker_exists: checks py.typed file exists
   - test_directory_structure: checks all 8 required dirs at repo root
   - test_swingrl_subpackages_exist: checks all 8 subpackage dirs have __init__.py

4. Run `uv run pytest tests/test_smoke.py -v` and confirm all tests pass (MPS test may skip in non-Mac CI but must pass on M1 Mac).
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke.py -v</automated>
  </verify>
  <done>
    - All smoke tests pass on M1 Mac
    - test_torch_mps_available_on_mac passes (not skipped) on Mac
    - test_directory_structure validates all 8 directories
    - test_swingrl_subpackages_exist validates all 8 subpackages
    - tests/ subdirectory mirror structure exists
  </done>
</task>

</tasks>

<verification>
1. `uv sync --locked` succeeds without error
2. `uv run pytest tests/test_smoke.py -v` — all tests green
3. `uv run python -c "import torch; print(torch.backends.mps.is_available())"` returns True on Mac
4. `ls src/swingrl/*/` confirms all subpackages exist with __init__.py
5. `cat src/swingrl/py.typed` exists (empty file)
</verification>

<success_criteria>
- pyproject.toml with all project deps resolves cleanly via uv
- uv.lock committed and reproducible
- Canonical directory structure complete
- swingrl package importable with all subpackages
- All smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-dev-foundation/01-01-SUMMARY.md`
</output>
