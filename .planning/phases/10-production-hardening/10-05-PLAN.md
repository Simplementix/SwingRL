---
phase: 10-production-hardening
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/swingrl/sentiment/__init__.py
  - src/swingrl/sentiment/finbert.py
  - src/swingrl/sentiment/news_fetcher.py
  - src/swingrl/features/pipeline.py
  - tests/sentiment/__init__.py
  - tests/sentiment/test_finbert.py
  - tests/sentiment/test_ab_experiment.py
autonomous: true
requirements: [HARD-03, HARD-04]

must_haves:
  truths:
    - "FinBERT scores financial headlines and returns sentiment score + confidence per headline"
    - "FinBERT model is lazy-loaded only when sentiment.enabled=True (not at import time)"
    - "News fetcher retrieves headlines from Alpaca News API with Finnhub fallback"
    - "Sentiment features integrate into the observation assembler when enabled"
    - "A/B comparison rejects sentiment if Sharpe improvement < 0.05"
  artifacts:
    - path: "src/swingrl/sentiment/finbert.py"
      provides: "Lazy-loaded FinBERT sentiment scorer"
      exports: ["FinBERTScorer"]
      contains: "_ensure_loaded"
    - path: "src/swingrl/sentiment/news_fetcher.py"
      provides: "News headline fetcher with Alpaca primary and Finnhub fallback"
      exports: ["NewsFetcher"]
    - path: "tests/sentiment/test_finbert.py"
      provides: "Tests for FinBERT scoring with mocked model"
    - path: "tests/sentiment/test_ab_experiment.py"
      provides: "Tests for A/B experiment Sharpe threshold comparison"
  key_links:
    - from: "src/swingrl/sentiment/finbert.py"
      to: "transformers"
      via: "lazy import of AutoModelForSequenceClassification"
      pattern: "from transformers import"
    - from: "src/swingrl/sentiment/news_fetcher.py"
      to: "alpaca-py"
      via: "News API client"
      pattern: "alpaca"
    - from: "src/swingrl/features/pipeline.py"
      to: "src/swingrl/sentiment/finbert.py"
      via: "conditional import when sentiment.enabled"
      pattern: "sentiment\\.enabled"
---

<objective>
Implement FinBERT sentiment pipeline: lazy-loaded model scoring headlines from Alpaca News + Finnhub, with A/B experiment infrastructure using +0.05 Sharpe threshold.

Purpose: Test whether financial sentiment features improve trading performance before permanently adding them.
Output: sentiment/ module with FinBERT scorer, news fetcher, feature pipeline integration.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-production-hardening/10-CONTEXT.md
@.planning/phases/10-production-hardening/10-RESEARCH.md
@.planning/phases/10-production-hardening/10-01-SUMMARY.md

<interfaces>
From src/swingrl/config/schema.py (Plan 01):
```python
class SentimentConfig(BaseModel):
    enabled: bool = Field(default=False)
    model_name: str = Field(default="ProsusAI/finbert")
    max_headlines_per_asset: int = Field(default=10, ge=1)
    finnhub_api_key: str = Field(default="")
```

From src/swingrl/features/pipeline.py:
```python
class FeaturePipeline:
    def compute_observation(self, env_name: str) -> np.ndarray: ...
```

From src/swingrl/utils/retry.py (Plan 01):
```python
def swingrl_retry(max_attempts=5, min_wait=1, max_wait=30, retryable_exceptions=...): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FinBERT scorer and news fetcher</name>
  <files>src/swingrl/sentiment/__init__.py, src/swingrl/sentiment/finbert.py, src/swingrl/sentiment/news_fetcher.py, tests/sentiment/__init__.py, tests/sentiment/test_finbert.py</files>
  <behavior>
    - Test: FinBERTScorer does not load model at __init__ (lazy loading)
    - Test: score_headlines returns list of dicts with "positive", "negative", "neutral" scores summing to ~1.0
    - Test: score_headlines handles empty list (returns empty list)
    - Test: aggregate_sentiment returns mean sentiment score and mean confidence for a batch
    - Test: NewsFetcher.fetch_headlines returns list of headline strings for a symbol
    - Test: NewsFetcher falls back to Finnhub when Alpaca fails
    - Test: NewsFetcher respects max_headlines_per_asset limit
    - Test: NewsFetcher returns empty list when both sources fail (no crash)
  </behavior>
  <action>
    1. Create src/swingrl/sentiment/__init__.py (empty).
    2. Create tests/sentiment/__init__.py (empty).

    3. Create src/swingrl/sentiment/finbert.py:
       - `FinBERTScorer` class with lazy loading pattern:
         - `__init__(self, model_name: str = "ProsusAI/finbert")`: stores model_name, sets _model and _tokenizer to None
         - `_ensure_loaded(self) -> None`: Lazy import of transformers, load model and tokenizer only on first use. Log "finbert_model_loaded" on success.
         - `score_headlines(self, headlines: list[str]) -> list[dict[str, float]]`: Batch tokenize, run inference, softmax to get {"positive": f, "negative": f, "neutral": f} per headline. Handle empty list edge case.
         - `aggregate_sentiment(self, scores: list[dict[str, float]]) -> tuple[float, float]`: Compute mean sentiment_score (positive - negative) and mean confidence (max of 3 probs). Returns (score, confidence).

    4. Create src/swingrl/sentiment/news_fetcher.py:
       - `NewsFetcher` class:
         - `__init__(self, config: SentimentConfig, alpaca_api_key: str = "", alpaca_api_secret: str = "")`: Store config, create Alpaca News client if keys available.
         - `fetch_headlines(self, symbol: str, lookback_hours: int = 24) -> list[str]`: Try Alpaca News API first (primary). If fails, try Finnhub via config.finnhub_api_key (secondary). Respect max_headlines_per_asset limit. Use swingrl_retry decorator on API calls. Return empty list on total failure with log warning.
         - `fetch_all_assets(self, symbols: list[str]) -> dict[str, list[str]]`: Fetch headlines for all symbols, return {symbol: [headlines]}.

    5. Create tests/sentiment/test_finbert.py:
       - Mock transformers imports (don't require actual model download in tests)
       - Mock the model's forward pass to return known logits
       - Test lazy loading (assert _model is None before first score_headlines call)
       - Test score_headlines returns correct format
       - Test aggregate_sentiment computes mean correctly
       - Test NewsFetcher with mocked HTTP responses for Alpaca and Finnhub
  </action>
  <verify>
    <automated>uv run pytest tests/sentiment/test_finbert.py -x -v</automated>
  </verify>
  <done>FinBERTScorer lazy-loads model and scores headlines, NewsFetcher retrieves from Alpaca with Finnhub fallback, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A/B experiment and feature pipeline integration</name>
  <files>src/swingrl/features/pipeline.py, tests/sentiment/test_ab_experiment.py</files>
  <behavior>
    - Test: When sentiment.enabled=False, observation vector has original dimensions (156 equity, 45 crypto)
    - Test: When sentiment.enabled=True, observation vector gains 2 features per asset (sentiment_score, confidence)
    - Test: A/B comparison with Sharpe improvement >= 0.05 recommends keeping sentiment
    - Test: A/B comparison with Sharpe improvement < 0.05 recommends disabling sentiment
    - Test: Sentiment features are NaN-safe (missing headlines produce 0.0 defaults)
  </behavior>
  <action>
    1. Modify src/swingrl/features/pipeline.py to conditionally include sentiment features:
       - In compute_observation() (or the observation assembly function), check config.sentiment.enabled
       - If enabled: instantiate FinBERTScorer (lazy singleton), fetch headlines for current symbols via NewsFetcher, score headlines, aggregate to (sentiment_score, confidence) per asset, append to observation vector
       - If disabled: skip entirely, observation dimensions unchanged
       - Handle exceptions gracefully: if sentiment scoring fails, use default values (0.0, 0.0) -- never crash the pipeline

    2. Create tests/sentiment/test_ab_experiment.py:
       - Test the A/B comparison logic: given two sets of backtest results (with/without sentiment), apply the +0.05 Sharpe threshold from FEAT-08 protocol
       - Test: mock backtest results where sentiment model has Sharpe 0.8 vs baseline 0.7 -> keep (improvement = 0.1 > 0.05)
       - Test: mock backtest results where sentiment model has Sharpe 0.72 vs baseline 0.7 -> discard (improvement = 0.02 < 0.05)
       - This reuses the existing compare_features pattern from Phase 5's feature pipeline

    Note: The actual A/B experiment execution is a manual training process -- this plan provides the infrastructure (toggle, integration, comparison threshold). The user runs training with sentiment.enabled=True and sentiment.enabled=False, then compares results.
  </action>
  <verify>
    <automated>uv run pytest tests/sentiment/ -x -v</automated>
  </verify>
  <done>Sentiment features conditionally added to observation vector, A/B comparison uses +0.05 Sharpe threshold, all tests pass</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/sentiment/ -x -v` passes all sentiment tests
- FinBERT model is NOT loaded at import time (lazy loading verified)
- Observation dimensions unchanged when sentiment.enabled=False
- A/B threshold matches FEAT-08 protocol (+0.05)
</verification>

<success_criteria>
- FinBERT lazy-loads only when needed, scores headlines into sentiment_score + confidence
- News fetcher uses Alpaca News (primary) with Finnhub fallback
- Feature pipeline conditionally includes sentiment features based on config toggle
- A/B experiment infrastructure uses +0.05 Sharpe improvement threshold
- All sentiment failures are gracefully handled (no pipeline crashes)
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-05-SUMMARY.md`
</output>
