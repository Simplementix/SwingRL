---
phase: 18-data-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/swingrl/data/verification.py
  - tests/data/test_verification.py
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "Operator reads data/verification.json to confirm all quality gates passed"
    - "Verification detects missing equity symbols in DuckDB"
    - "Verification detects NaN in observation vectors"
    - "Verification detects crypto timestamp gaps >8 hours in persisted ohlcv_4h data"
    - "Console output shows [PASS]/[FAIL] per check for each run"
  artifacts:
    - path: "src/swingrl/data/verification.py"
      provides: "DuckDB verification queries + JSON report writer"
      exports: ["run_verification", "write_report", "VerificationResult", "CheckResult"]
      min_lines: 140
    - path: "tests/data/test_verification.py"
      provides: "Unit tests for verification module"
      min_lines: 100
  key_links:
    - from: "src/swingrl/data/verification.py"
      to: "src/swingrl/data/db.py"
      via: "DatabaseManager.duckdb() context manager"
      pattern: "db\\.duckdb\\(\\)"
    - from: "src/swingrl/data/verification.py"
      to: "src/swingrl/features/pipeline.py"
      via: "FeaturePipeline.get_observation()"
      pattern: "get_observation"
    - from: "src/swingrl/data/verification.py"
      to: "ohlcv_4h table"
      via: "_check_crypto_gaps diffs consecutive timestamps"
      pattern: "_check_crypto_gaps"
---

<objective>
Build the data verification module that queries DuckDB after ingestion and feature computation to confirm all quality gates pass: row counts, date coverage, crypto gap detection, NaN checks, and observation vector shapes.

Purpose: DATA-04 requires zero NaN in observation vectors and no unexpected date gaps in crypto. The verification module is the quality gate that confirms this and produces a machine-readable JSON report.
Output: `src/swingrl/data/verification.py` with tests.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-data-ingestion/18-CONTEXT.md
@.planning/phases/18-data-ingestion/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/swingrl/data/db.py:
```python
class DatabaseManager:
    def __new__(cls, config: SwingRLConfig | None = None) -> DatabaseManager: ...
    @classmethod
    def reset(cls) -> None: ...
    def duckdb(self) -> Generator[Any, None, None]: ...  # context manager yielding cursor
    def init_schema(self) -> None: ...
```

From src/swingrl/features/pipeline.py:
```python
class FeaturePipeline:
    def __init__(self, config: SwingRLConfig, conn: Any) -> None: ...
    def compute_equity(self, start: str | None = None, end: str | None = None) -> None: ...
    def compute_crypto(self, start: str | None = None, end: str | None = None) -> None: ...
    def get_observation(self, environment: str, date_str: str) -> np.ndarray: ...
    # environment: "equity" -> 156-dim, "crypto" -> 45-dim
```

From src/swingrl/config/schema.py:
```python
class SwingRLConfig(BaseModel):
    equity: EquityConfig        # equity.symbols: list[str] — 8 ETFs
    crypto: CryptoConfig        # crypto.symbols: list[str] — ["BTCUSDT", "ETHUSDT"]
    fred: FREDConfig            # fred.series: list[str] — ["VIXCLS", "T10Y2Y", "DFF", "CPIAUCSL", "UNRATE"]
```

From src/swingrl/utils/exceptions.py:
```python
class DataError(SwingRLError): ...
```

NOTE: An existing `src/swingrl/data/validation.py` (340 lines) contains `DataValidator` for per-batch OHLCV validation (staleness, gaps, etc.). This is DIFFERENT from the verification module being created here. The new file name must NOT conflict — use a different name. Since the RESEARCH.md says `verification.py`, but the CONTEXT.md says "verification suite", we should create a NEW file. However, looking at the existing project, `validation.py` already exists. The new module should be named `verify.py` or we can extend `verification.py` if that name is free. Since the research explicitly uses `verification.py` as the filename AND no file by that name exists in `src/swingrl/data/`, we proceed with `verification.py`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dataclasses, report writer, and date/row check functions (RED then GREEN)</name>
  <files>src/swingrl/data/verification.py, tests/data/test_verification.py</files>
  <behavior>
    - Test: VerificationResult and CheckResult dataclasses can be constructed and serialized to dict
    - Test: _check_equity_rows returns passed=False when DuckDB ohlcv_daily has no rows for a configured symbol
    - Test: _check_equity_rows returns passed=True when all configured equity symbols have rows (>100)
    - Test: _check_crypto_rows returns passed=False when ohlcv_4h is missing a configured crypto symbol
    - Test: _check_macro_series returns passed=False when a configured FRED series is missing from macro_features
    - Test: write_report writes valid JSON to the specified path with correct structure
    - Test: print_summary outputs [PASS]/[FAIL] per check to stdout (capture with capsys)
  </behavior>
  <action>
    **Step A — RED commit:** Create `tests/data/test_verification.py` FIRST with all 7 test functions listed in behavior. Tests should use an in-memory or tmp-path DuckDB. Use `tmp_config` fixture from conftest.py. Call `DatabaseManager.reset()` in teardown. Run tests — they MUST fail (imports will fail since verification.py does not exist). Commit: `test(18-01): add failing tests for verification dataclasses and row checks`.

    **Step B — GREEN commit:** Create `src/swingrl/data/verification.py` with:

    1. `VerificationResult` and `CheckResult` dataclasses:
       ```python
       @dataclass
       class CheckResult:
           name: str
           passed: bool
           detail: str

       @dataclass
       class VerificationResult:
           passed: bool
           checks: list[CheckResult]
       ```

    2. Row/series check functions (all take DuckDB cursor + SwingRLConfig):
       - `_check_equity_rows(cursor, config)` — `SELECT symbol, COUNT(*) FROM ohlcv_daily GROUP BY symbol`, verify all `config.equity.symbols` present with >100 rows each
       - `_check_crypto_rows(cursor, config)` — same for `ohlcv_4h` and `config.crypto.symbols`
       - `_check_macro_series(cursor, config)` — `SELECT DISTINCT series_id FROM macro_features`, verify all `config.fred.series` present
       - `_check_equity_date_range(cursor, config)` — `SELECT MIN(date), MAX(date) FROM ohlcv_daily`, log coverage
       - `_check_crypto_date_range(cursor, config)` — same for `ohlcv_4h` with datetime column

    3. `write_report(result: VerificationResult, path: Path) -> None`:
       - `path.parent.mkdir(parents=True, exist_ok=True)`
       - Write `json.dumps(dataclasses.asdict(result), indent=2, default=str)`

    4. `print_summary(result: VerificationResult) -> None`:
       - Print each check: `[PASS]` or `[FAIL]` + name + detail
       - Print overall result at end

    Run tests — all 7 MUST pass. Commit: `feat(18-01): implement verification dataclasses and row checks`.

    Follow all CLAUDE.md conventions: `from __future__ import annotations`, structlog logging, type hints, docstrings.
  </action>
  <verify>
    <automated>cd /Users/varunpanchal/Documents/Projects/Simplementix/SwingRL && uv run pytest tests/data/test_verification.py -v -x -k "not obs_vector and not crypto_gaps and not run_verification"</automated>
  </verify>
  <done>
    - VerificationResult/CheckResult dataclasses exported and serializable
    - Row/series check functions detect missing symbols and series
    - write_report produces valid JSON at specified path
    - print_summary outputs human-readable [PASS]/[FAIL] per check
    - All 7 tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add crypto gap detection, obs vector checks, and run_verification aggregator (RED then GREEN)</name>
  <files>src/swingrl/data/verification.py, tests/data/test_verification.py</files>
  <behavior>
    - Test: _check_crypto_gaps returns passed=True when consecutive ohlcv_4h timestamps are all <=8 hours apart
    - Test: _check_crypto_gaps returns passed=False when ohlcv_4h has a gap >8 hours (>2 bars) for any symbol
    - Test: _check_obs_vector returns passed=False when observation vector contains NaN
    - Test: _check_obs_vector returns passed=True and validates shape (156-dim equity, 45-dim crypto) when vector is clean
    - Test: run_verification returns VerificationResult with overall passed=False if any check fails
    - Test: run_verification returns passed=True when all checks pass
  </behavior>
  <action>
    **Step A — RED commit:** Add 6 new test functions to `tests/data/test_verification.py` for the behaviors listed above. For crypto gap tests, insert rows into an in-memory DuckDB ohlcv_4h table with known timestamps (some with gaps >8h, some without). For obs vector tests, mock `FeaturePipeline.get_observation()` to return controlled numpy arrays. For run_verification tests, mock DatabaseManager and FeaturePipeline. Run new tests — they MUST fail. Commit: `test(18-01): add failing tests for crypto gaps, obs vectors, and aggregator`.

    **Step B — GREEN commit:** Add to `src/swingrl/data/verification.py`:

    1. `_check_crypto_gaps(cursor, config) -> CheckResult`:
       - For each symbol in `config.crypto.symbols`:
         `SELECT timestamp FROM ohlcv_4h WHERE symbol = ? ORDER BY timestamp`
       - Compute consecutive timestamp differences
       - Flag any gap > 8 hours (timedelta(hours=8))
       - Return passed=False with detail listing the gap location if any gap exceeds threshold
       - Return passed=True with detail "No gaps >8h found" if clean

    2. `_check_obs_vector(pipeline, environment, date_str) -> CheckResult`:
       - Call `pipeline.get_observation(environment, date_str)`
       - Assert shape matches expected dim (equity=156, crypto=45) and `np.isnan(obs).sum() == 0`
       - Return CheckResult with pass/fail

    3. `run_verification(config: SwingRLConfig) -> VerificationResult`:
       - Create DatabaseManager, open DuckDB cursor
       - Run ALL checks (equity rows, crypto rows, macro series, equity date range, crypto date range, crypto gaps, obs vector equity, obs vector crypto)
       - Create FeaturePipeline with the cursor for obs vector checks
       - Query MAX date from relevant feature tables for obs vector date_str
       - Return VerificationResult with `passed = all(c.passed for c in checks)`

    Run all tests — MUST pass. Commit: `feat(18-01): implement crypto gaps, obs vectors, and run_verification`.

    Follow all CLAUDE.md conventions. Mock `FeaturePipeline` in tests rather than constructing real feature data.
  </action>
  <verify>
    <automated>cd /Users/varunpanchal/Documents/Projects/Simplementix/SwingRL && uv run pytest tests/data/test_verification.py -v -x && uv run ruff check src/swingrl/data/verification.py</automated>
  </verify>
  <done>
    - _check_crypto_gaps detects gaps >8 hours in persisted ohlcv_4h timestamps
    - _check_obs_vector validates shape and NaN-free observation vectors
    - run_verification aggregates all checks into single VerificationResult
    - All tests pass (both Task 1 and Task 2 tests)
    - Ruff reports no lint errors
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/data/test_verification.py -v -x` — all verification tests pass
- `uv run python -c "from swingrl.data.verification import run_verification, write_report, VerificationResult, CheckResult"` — imports succeed
- `uv run ruff check src/swingrl/data/verification.py` — no lint errors
</verification>

<success_criteria>
- Verification module exports run_verification, write_report, VerificationResult, CheckResult
- All individual checks testable in isolation with mock DuckDB data
- Crypto gap detection flags any persisted ohlcv_4h gap >8 hours
- JSON report format matches the dataclass structure
- Console summary clearly shows [PASS]/[FAIL] per check
</success_criteria>

<output>
After completion, create `.planning/phases/18-data-ingestion/18-01-SUMMARY.md`
</output>
