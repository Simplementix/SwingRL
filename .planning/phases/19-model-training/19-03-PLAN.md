---
phase: 19-model-training
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - scripts/validate_memory.py
  - scripts/seed_memory_from_backtest.py
autonomous: false
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-06
  - TRAIN-07

must_haves:
  truths:
    - "All 3 algorithms complete training for equity and crypto on homelab CPU"
    - "Walk-forward validation records Sharpe, MDD, and profit factor for each algorithm"
    - "Ensemble Sharpe > 1.0 and MDD < 15% before any deployment"
    - "Each trained model.zip has a corresponding vec_normalize.pkl in models/active/{env}/{algo}/"
    - "Training report JSON contains fold-by-fold results, ensemble weights, and timing"
    - "Memory validation script confirms DuckDB schema and training data integrity"
  artifacts:
    - path: "scripts/validate_memory.py"
      provides: "Pre-live validation script for memory agent data integrity"
    - path: "scripts/seed_memory_from_backtest.py"
      provides: "Seeds memory agent with backtest fill records for live trading context"
    - path: "data/training_report.json"
      provides: "Structured training report with fold results, ensemble weights, gate results, timing"
    - path: "models/active/equity/ppo/model.zip"
      provides: "Trained PPO model for equity"
    - path: "models/active/equity/a2c/model.zip"
      provides: "Trained A2C model for equity"
    - path: "models/active/equity/sac/model.zip"
      provides: "Trained SAC model for equity"
    - path: "models/active/crypto/ppo/model.zip"
      provides: "Trained PPO model for crypto"
    - path: "models/active/crypto/a2c/model.zip"
      provides: "Trained A2C model for crypto"
    - path: "models/active/crypto/sac/model.zip"
      provides: "Trained SAC model for crypto"
  key_links:
    - from: "scripts/train_pipeline.py"
      to: "models/active/"
      via: "TrainingOrchestrator._save_model() writes model.zip + vec_normalize.pkl"
      pattern: "models/active"
    - from: "scripts/train_pipeline.py"
      to: "data/training_report.json"
      via: "write_training_report() at pipeline completion"
      pattern: "training_report\\.json"
---

<objective>
Create memory validation and backtest seeding scripts, then execute the full training pipeline on the homelab and verify results.

Purpose: This plan produces the actual trained models that will be used for paper trading in Phase 20. It also creates utility scripts for memory agent validation and backtest seeding that are needed before live trading.

Output: validate_memory.py, seed_memory_from_backtest.py scripts. Trained models in models/active/. Training report JSON.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-model-training/19-01-SUMMARY.md
@.planning/phases/19-model-training/19-02-SUMMARY.md

@scripts/train_pipeline.py
@memory_storage_spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory validation and backtest seeding scripts</name>
  <files>
    scripts/validate_memory.py
    scripts/seed_memory_from_backtest.py
  </files>
  <action>
    1. Create scripts/validate_memory.py -- automated pre-live validation script per CONTEXT.md:
       - Check DuckDB schema: verify training_epochs, meta_decisions, reward_adjustments tables exist
       - Check hmm_state_history has p_crisis column (3-state schema)
       - Check training_runs table has meta-training columns (if ALTER TABLE applied)
       - Count rows in each table, report gaps
       - Verify at least 3 training run summaries exist (cold-start guard threshold)
       - If memory agent is reachable (--memory-url flag): check /health endpoint, run curl-style queries from memory_storage_spec.md Section 8
       - Exit 0 if all checks pass, exit 1 with diagnostic if any fail
       - CLI: `python scripts/validate_memory.py --config config/swingrl.yaml [--memory-url http://localhost:8889]`
       - Use structlog, pathlib, load_config(). Follow CLAUDE.md conventions.

    2. Create scripts/seed_memory_from_backtest.py -- seeds memory agent with backtest fill records:
       - Implement per memory_storage_spec.md Section 4 code example
       - Queries fills from SQLite (trading_ops.db) joined with training_runs
       - Joins HMM regime from DuckDB hmm_state_history (adjusted for 3-state: p_bull, p_bear, p_crisis)
       - Formats as plain text and ingests via MemoryClient.ingest()
       - Triggers consolidation after all fills ingested
       - CLI: `python scripts/seed_memory_from_backtest.py --config config/swingrl.yaml --memory-url http://localhost:8889`
       - Gracefully handles missing tables (no fills yet = no seeding, exit 0 with message)
       - Use structlog, pathlib, load_config(). Follow CLAUDE.md conventions.
  </action>
  <verify>
    <automated>uv run python scripts/validate_memory.py --config config/swingrl.yaml 2>&1 || true</automated>
  </verify>
  <done>
    validate_memory.py runs DuckDB schema and data integrity checks. seed_memory_from_backtest.py seeds memory with backtest fills. Both scripts follow CLAUDE.md conventions and handle missing data gracefully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Homelab training execution and verification</name>
  <files>models/active/</files>
  <action>
    Execute the full training pipeline on the homelab via Docker exec and verify all models are deployed with passing ensemble gates.

    Steps Claude will perform before this checkpoint:
    1. Push the feature branch to origin
    2. SSH to homelab, checkout the branch, pull latest
    3. Run: `docker exec swingrl python scripts/train_pipeline.py --env all`
    4. Monitor progress via logs (this may take 12-48 hours on CPU)
    5. After completion, check model deployment and training report
  </action>
  <verify>
    <automated>ssh homelab "ls ~/swingrl/models/active/equity/*/model.zip ~/swingrl/models/active/crypto/*/model.zip 2>/dev/null | wc -l" | grep -q 6</automated>
  </verify>
  <done>
    All 6 models deployed (3 algos x 2 envs). Ensemble gate passed for both environments. Training report JSON written.
  </done>
</task>

</tasks>

<verification>
ssh homelab "ls ~/swingrl/models/active/equity/*/model.zip ~/swingrl/models/active/crypto/*/model.zip" 2>/dev/null | wc -l
# Expected: 6 (3 equity + 3 crypto model files)

ssh homelab "cd ~/swingrl && python -c \"import json; r=json.load(open('data/training_report.json')); print('equity gate:', r['equity']['ensemble_gate']); print('crypto gate:', r['crypto']['ensemble_gate'])\""
# Expected: Both gates passed=True
</verification>

<success_criteria>
- 6 model.zip files exist in models/active/ (3 algos x 2 envs)
- 6 vec_normalize.pkl files exist alongside models
- Ensemble Sharpe > 1.0 for both environments
- Ensemble MDD < 15% for both environments
- data/training_report.json contains fold-by-fold results and ensemble weights
- validate_memory.py passes schema checks
- Training completed on homelab CPU without MPS dependency
</success_criteria>

<output>
After completion, create `.planning/phases/19-model-training/19-03-SUMMARY.md`
</output>
