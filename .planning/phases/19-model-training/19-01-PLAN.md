---
phase: 19-model-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/swingrl/config/schema.py
  - config/swingrl.yaml
  - src/swingrl/memory/__init__.py
  - src/swingrl/memory/client.py
  - src/swingrl/memory/training/__init__.py
  - src/swingrl/memory/training/bounds.py
  - src/swingrl/training/trainer.py
  - src/swingrl/training/pipeline_helpers.py
  - tests/training/test_pipeline_helpers.py
  - tests/memory/test_bounds.py
  - tests/memory/__init__.py
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-04
  - TRAIN-05

must_haves:
  truths:
    - "Existing CI passes with memory_agent disabled defaults -- no regressions from adding MemoryAgentConfig to SwingRLConfig"
    - "All LLM hyperparameter suggestions are bounds-checked before reaching the trainer (clamp_run_config and clamp_reward_weights enforce ranges)"
    - "Plan 02 executor can import MemoryClient, clamp_run_config, slice_recent, check_ensemble_gate, compute_ensemble_weights_from_wf, TUNING_GRID, and decide_final_timesteps without additional setup"
    - "TrainingOrchestrator.train() accepts hyperparams_override so Plan 02 can pass tuning-round configs"
    - "Pipeline helpers produce correct data slices and gate decisions (verified by tests/training/test_pipeline_helpers.py)"
  artifacts:
    - path: "src/swingrl/config/schema.py"
      provides: "MemoryAgentConfig + MemoryLiveEndpointsConfig Pydantic models added to SwingRLConfig"
      contains: "class MemoryAgentConfig"
    - path: "src/swingrl/memory/client.py"
      provides: "MemoryClient with ingest(), ingest_training(), consolidate()"
      exports: ["MemoryClient"]
    - path: "src/swingrl/memory/training/bounds.py"
      provides: "clamp_run_config(), clamp_reward_weights() with HYPERPARAM_BOUNDS and REWARD_BOUNDS"
      exports: ["clamp_run_config", "clamp_reward_weights"]
    - path: "src/swingrl/training/pipeline_helpers.py"
      provides: "slice_recent(), check_ensemble_gate(), compute_ensemble_weights_from_wf(), TUNING_GRID, decide_final_timesteps()"
      exports: ["slice_recent", "check_ensemble_gate", "compute_ensemble_weights_from_wf", "TUNING_GRID", "decide_final_timesteps"]
    - path: "src/swingrl/training/trainer.py"
      provides: "hyperparams_override parameter on TrainingOrchestrator.train()"
      contains: "hyperparams_override"
  key_links:
    - from: "src/swingrl/training/pipeline_helpers.py"
      to: "src/swingrl/training/ensemble.py"
      via: "EnsembleBlender.compute_weights() called with real walk-forward Sharpe"
      pattern: "blender\\.compute_weights"
    - from: "src/swingrl/memory/training/bounds.py"
      to: "src/swingrl/training/trainer.py"
      via: "Clamped hyperparams passed as hyperparams_override to train()"
      pattern: "hyperparams_override"
---

<objective>
Create the memory agent foundation layer (config, client, bounds) and training pipeline helper functions that subsequent plans depend on. Add hyperparams_override to TrainingOrchestrator for tuning support.

Purpose: Establishes the config schema, memory client, safety bounds, and all pure-function helpers needed by the train_pipeline.py orchestrator (Plan 02) and memory training modules (Plan 02). No orchestration logic yet -- just the building blocks.

Output: 5 new files (config additions, memory client, bounds, pipeline helpers, tests), 2 modified files (schema.py, trainer.py)
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/swingrl/config/schema.py
@src/swingrl/training/trainer.py
@src/swingrl/training/ensemble.py
@src/swingrl/agents/backtest.py
@src/swingrl/agents/validation.py

<interfaces>
<!-- Key types the executor needs -->

From src/swingrl/training/trainer.py:
```python
HYPERPARAMS: dict[str, dict[str, Any]]  # ppo/a2c/sac locked defaults
class TrainingResult:
    model_path: Path
    vec_normalize_path: Path
    env_name: str
    algo_name: str
    converged_at_step: int | None
    total_timesteps: int

class TrainingOrchestrator:
    def train(self, env_name, algo_name, features, prices, total_timesteps=1_000_000) -> TrainingResult
```

From src/swingrl/training/ensemble.py:
```python
def sharpe_softmax_weights(sharpe_ratios: dict[str, float]) -> dict[str, float]
class EnsembleBlender:
    def compute_weights(self, env_name: str, sharpe_ratios: dict[str, float]) -> dict[str, float]
```

From src/swingrl/agents/backtest.py:
```python
@dataclass
class FoldResult:
    fold_number: int
    train_range: tuple[int, int]
    test_range: tuple[int, int]
    in_sample_metrics: dict[str, float]
    out_of_sample_metrics: dict[str, float]
    trades: list[dict[str, Any]]
    gate_result: GateResult
    overfitting: dict[str, Any]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memory agent config, client, and bounds</name>
  <files>
    src/swingrl/config/schema.py
    config/swingrl.yaml
    src/swingrl/memory/__init__.py
    src/swingrl/memory/client.py
    src/swingrl/memory/training/__init__.py
    src/swingrl/memory/training/bounds.py
    tests/memory/__init__.py
    tests/memory/test_bounds.py
  </files>
  <behavior>
    - clamp_run_config clamps learning_rate outside [1e-5, 1e-3] to nearest bound
    - clamp_run_config forces batch_size to nearest power of 2
    - clamp_run_config does not mutate input dict
    - clamp_reward_weights clamps each weight to its range and renormalizes to sum=1.0
    - clamp_reward_weights returns safe defaults when total=0
    - MemoryClient.ingest_training() asserts source ends with ':historical'
    - MemoryClient.ingest() returns False on connection error (fail-open)
    - MemoryAgentConfig defaults: enabled=False, meta_training=False, all live_endpoints=False
    - SwingRLConfig with memory_agent section loads without error from existing config
  </behavior>
  <action>
    1. Add MemoryLiveEndpointsConfig and MemoryAgentConfig Pydantic models to schema.py (before SwingRLConfig). Add `memory_agent: MemoryAgentConfig = Field(default_factory=MemoryAgentConfig)` to SwingRLConfig. Use exact field names from memory_meta_trainer.md Section 0 but update ollama_smart_model to "qwen3:14b" per CONTEXT.md. All defaults disabled so existing CI/tests unaffected.

    2. Add `memory_agent:` section to config/swingrl.yaml with `enabled: false` and `meta_training: false`. All live endpoints false. base_url: "http://swingrl-memory:8889".

    3. Create src/swingrl/memory/__init__.py (empty) and src/swingrl/memory/training/__init__.py (empty).

    4. Create src/swingrl/memory/client.py with MemoryClient class. Three methods:
       - ingest(payload, timeout=5.0) -> bool: POST to /ingest, returns False on any error
       - ingest_training(text, source, timeout=5.0) -> bool: asserts source.endswith(":historical"), delegates to ingest()
       - consolidate(timeout=60.0) -> bool: POST to /consolidate
       Use structlog (not stdlib logging) per CLAUDE.md. Use `from __future__ import annotations`.

    5. Create src/swingrl/memory/training/bounds.py with exact constants from memory_meta_trainer.md Section 1:
       - HYPERPARAM_BOUNDS, REWARD_BOUNDS, MIN_EPOCHS_BEFORE_STOP=10, MAX_EPOCHS=200, MIN_WINDOW_YEARS=1.0, CRISIS_PERIOD_PCT=(0.05, 0.50)
       - clamp_run_config(config: dict) -> dict
       - clamp_reward_weights(weights: dict) -> dict
       - _nearest_power_of_two(n: int) -> int
       Use structlog. Follow CLAUDE.md conventions.

    6. Create tests/memory/__init__.py (empty) and tests/memory/test_bounds.py with tests for clamp_run_config and clamp_reward_weights covering the behavior list above.
  </action>
  <verify>
    <automated>uv run pytest tests/memory/test_bounds.py -v -x</automated>
  </verify>
  <done>
    MemoryAgentConfig in SwingRLConfig. MemoryClient with fail-open ingest. bounds.py clamps hyperparams and reward weights. All tests pass. Existing test suite unbroken.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hyperparams override + pipeline helper functions</name>
  <files>
    src/swingrl/training/trainer.py
    src/swingrl/training/pipeline_helpers.py
    tests/training/test_pipeline_helpers.py
  </files>
  <behavior>
    - TrainingOrchestrator.train() accepts optional hyperparams_override dict
    - hyperparams_override merges into HYPERPARAMS copy (override wins)
    - slice_recent("equity", features, prices) returns last 756 bars (3yr x 252)
    - slice_recent("crypto", features, prices) returns last 2191 bars (1yr x 365.25 x 6)
    - slice_recent handles data shorter than window (returns all data)
    - check_ensemble_gate returns (True, sharpe, mdd) when sharpe>1.0 and mdd<0.15
    - check_ensemble_gate returns (False, sharpe, mdd) when sharpe<=1.0 or mdd>=0.15
    - check_ensemble_gate handles empty fold results gracefully
    - compute_ensemble_weights_from_wf produces real weights from walk-forward Sharpe (not placeholder 1.0)
    - decide_final_timesteps escalates to 2M/1M when converged_at_step is None
    - TUNING_GRID round 1 has PPO-only entries, round 2 has A2C+SAC entries
    - TUNING_GRID never changes net_arch (always [64,64])
  </behavior>
  <action>
    1. Modify TrainingOrchestrator.train() in trainer.py:
       - Add parameter: `hyperparams_override: dict[str, Any] | None = None`
       - After `params = HYPERPARAMS[algo_name].copy()`, add: `if hyperparams_override: params.update(hyperparams_override)`
       - Do NOT change the method's other behavior. Keep existing docstring but add hyperparams_override to Args.

    2. Create src/swingrl/training/pipeline_helpers.py with:
       - RECENT_WINDOW_BARS = {"equity": 252 * 3, "crypto": 2191} (756 and 2191)
       - DEFAULT_TIMESTEPS = {"equity": 1_000_000, "crypto": 500_000}
       - ESCALATED_TIMESTEPS = {"equity": 2_000_000, "crypto": 1_000_000}
       - TUNING_GRID: dict[int, dict[str, list[dict[str, Any]]]] with round 1 PPO-only (4 variants: lr x ent_coef x clip_range combos from RESEARCH.md) and round 2 A2C (2 lr variants) + SAC (2 lr variants)
       - slice_recent(features, prices, env_name) -> tuple[ndarray, ndarray]: slices to last RECENT_WINDOW_BARS[env_name] bars. If data shorter than window, returns all data.
       - check_ensemble_gate(all_fold_results: dict[str, list[FoldResult]]) -> tuple[bool, float, float]: mean OOS Sharpe and mean OOS MDD across all folds and all algos. Returns (passed, ensemble_sharpe, ensemble_mdd). Gate: sharpe > 1.0 AND abs(mdd) < 0.15.
       - compute_ensemble_weights_from_wf(config, wf_results: dict[str, list[FoldResult]], env_name) -> dict[str, float]: aggregates mean OOS Sharpe per algo, passes to EnsembleBlender.compute_weights().
       - decide_final_timesteps(env_name, result: TrainingResult) -> int: returns ESCALATED if converged_at_step is None, else DEFAULT.

       Use type hints, docstrings, structlog, `from __future__ import annotations`. Import FoldResult from swingrl.agents.backtest, EnsembleBlender from swingrl.training.ensemble, TrainingResult from swingrl.training.trainer.

    3. Create tests/training/test_pipeline_helpers.py with comprehensive tests for all functions listed in the behavior block above. Use mock FoldResult objects with controlled out_of_sample_metrics. Use mock config for ensemble weight computation.
  </action>
  <verify>
    <automated>uv run pytest tests/training/test_pipeline_helpers.py tests/training/test_trainer.py -v -x</automated>
  </verify>
  <done>
    TrainingOrchestrator supports hyperparams_override. All 5 pipeline helper functions implemented with tests. TUNING_GRID defines round 1 (PPO) and round 2 (A2C+SAC) variants. Existing trainer tests still pass.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/memory/ tests/training/ -v -x
uv run ruff check src/swingrl/memory/ src/swingrl/training/pipeline_helpers.py
uv run mypy src/swingrl/memory/ src/swingrl/training/pipeline_helpers.py --ignore-missing-imports
</verification>

<success_criteria>
- MemoryAgentConfig in SwingRLConfig with disabled defaults
- MemoryClient with fail-open ingest and :historical assertion
- bounds.py clamps hyperparams and reward weights within spec ranges
- TrainingOrchestrator.train() accepts hyperparams_override
- All 5 pipeline helper functions (slice_recent, check_ensemble_gate, compute_ensemble_weights_from_wf, TUNING_GRID, decide_final_timesteps) implemented and tested
- Existing test suite passes (no regressions)
- ruff and mypy clean
</success_criteria>

<output>
After completion, create `.planning/phases/19-model-training/19-01-SUMMARY.md`
</output>
