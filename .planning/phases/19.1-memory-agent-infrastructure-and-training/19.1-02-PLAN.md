---
phase: 19.1-memory-agent-infrastructure-and-training
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/swingrl/config/schema.py
  - src/swingrl/memory/client.py
  - src/swingrl/features/schema.py
  - config/swingrl.yaml
  - tests/memory/test_client.py
  - tests/test_memory_service.py
autonomous: true
requirements:
  - TRAIN-06
  - TRAIN-07

must_haves:
  truths:
    - "MemoryClient sends X-API-Key header on all requests when api_key is configured"
    - "DuckDB migrations create training_epochs, meta_decisions, reward_adjustments tables idempotently"
    - "training_runs table has run_type and meta_rationale columns added via ALTER"
    - "SwingRLConfig.training.sac_buffer_size is configurable (default 500_000)"
    - "config/swingrl.yaml has memory_agent.enabled: true and memory_agent.meta_training: true for training variant"
  artifacts:
    - path: "src/swingrl/memory/client.py"
      provides: "MemoryClient with api_key param and X-API-Key header"
    - path: "src/swingrl/config/schema.py"
      provides: "sac_buffer_size field in TrainingConfig or top-level"
    - path: "src/swingrl/features/schema.py"
      provides: "DuckDB DDL for training_epochs, meta_decisions, reward_adjustments tables"
    - path: "config/swingrl.yaml"
      provides: "Updated memory_agent config section"
    - path: "tests/memory/test_client.py"
      provides: "Tests for MemoryClient API key header"
  key_links:
    - from: "src/swingrl/memory/client.py"
      to: "services/memory/auth.py"
      via: "X-API-Key header in urllib requests"
      pattern: "X-API-Key"
    - from: "src/swingrl/features/schema.py"
      to: "scripts/validate_memory.py"
      via: "DDL constants used by schema validation"
      pattern: "training_epochs|meta_decisions|reward_adjustments"
---

<objective>
Add DuckDB schema migrations for training telemetry tables, update MemoryClient to send API key headers, and add SAC buffer_size config field.

Purpose: These are the shared infrastructure changes that both the memory service (Plan 01) and the multi-iteration training loop (Plan 03) depend on. DuckDB tables store per-epoch and per-decision telemetry. MemoryClient needs auth headers to talk to the secured memory service. SAC buffer_size must be configurable per CONTEXT.md decision (500K default).

Output: Updated config schema, MemoryClient with auth, DuckDB DDL constants, and tests.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-memory-agent-infrastructure-and-training/19.1-CONTEXT.md
@.planning/phases/19.1-memory-agent-infrastructure-and-training/19.1-RESEARCH.md

<interfaces>
<!-- Existing interfaces to modify -->

From src/swingrl/memory/client.py:
```python
class MemoryClient:
    def __init__(self, base_url: str = "http://swingrl-memory:8889", default_timeout: float = 5.0) -> None: ...
    # Uses urllib.request.Request with Content-Type header only
    # Needs: api_key param, X-API-Key header on all requests
```

From src/swingrl/config/schema.py:
```python
class MemoryAgentConfig(BaseModel):
    enabled: bool = False
    meta_training: bool = False
    base_url: str = "http://swingrl-memory:8889"
    timeout_sec: float = 5.0
    meta_training_timeout_sec: float = 30.0
    ollama_smart_model: str = "qwen3:14b"
    # Needs: api_key field
```

From src/swingrl/features/schema.py:
```python
# Contains _HMM_STATE_HISTORY_DDL, _MODEL_METADATA_DDL, etc.
# Needs: _TRAINING_EPOCHS_DDL, _META_DECISIONS_DDL, _REWARD_ADJUSTMENTS_DDL
# Needs: ALTER TABLE training_runs DDL for run_type and meta_rationale columns
```

From src/swingrl/memory/training/meta_orchestrator.py:
```python
# _query_run_config() uses urllib without auth headers
# Needs: pass api_key through to requests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryClient API key support + config schema updates</name>
  <files>
    src/swingrl/memory/client.py
    src/swingrl/config/schema.py
    config/swingrl.yaml
    tests/memory/test_client.py
  </files>
  <behavior>
    - Test: MemoryClient with api_key="secret" sends X-API-Key header on ingest()
    - Test: MemoryClient with api_key="secret" sends X-API-Key header on consolidate()
    - Test: MemoryClient with api_key="" (default) sends NO X-API-Key header (backward compatible)
    - Test: MemoryAgentConfig.api_key field exists with default ""
    - Test: SwingRLConfig has sac_buffer_size field accessible (default 500_000)
  </behavior>
  <action>
**src/swingrl/memory/client.py:**
- Add `api_key: str = ""` parameter to MemoryClient.__init__()
- Store as self._api_key
- In both ingest() and consolidate(): if self._api_key, add `"X-API-Key": self._api_key` to the urllib.request.Request headers dict
- Backward compatible: empty string = no auth header (existing tests and mock usage unaffected)

**src/swingrl/config/schema.py:**
- Add `api_key: str = ""` field to MemoryAgentConfig (sourced from MEMORY_API_KEY env var override: SWINGRL_MEMORY_AGENT__API_KEY)
- Add `sac_buffer_size: int = 500_000` field to TrainingConfig (or appropriate parent). Per CONTEXT.md: "Default 500K (configurable via SwingRLConfig). Proven 200K works; 500K fits with 24GB Ollama allocation"
- Ensure env var override works: SWINGRL_TRAINING__SAC_BUFFER_SIZE

**config/swingrl.yaml:**
- Update memory_agent section: add api_key: "" (populated from .env in production)
- Keep enabled: false and meta_training: false as defaults (training scripts override programmatically)

**tests/memory/test_client.py:**
- Add tests in new TestMemoryClientAuth class
- Use unittest.mock to patch urllib.request.urlopen and verify Request headers
- Test api_key="test-secret" -> X-API-Key header present
- Test api_key="" -> no X-API-Key header
- Test both ingest() and consolidate() methods
  </action>
  <verify>
    <automated>cd /Users/varunpanchal/Documents/Projects/Simplementix/SwingRL && uv run pytest tests/memory/test_client.py -v -x 2>&1 | tail -15</automated>
  </verify>
  <done>MemoryClient sends X-API-Key when api_key is set; MemoryAgentConfig has api_key field; sac_buffer_size is configurable at 500K default; existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DuckDB schema migrations for training telemetry tables</name>
  <files>
    src/swingrl/features/schema.py
    scripts/validate_memory.py
    tests/test_memory_service.py
  </files>
  <behavior>
    - Test: training_epochs table created with all columns (run_id, epoch, algo, env, sharpe, mdd, reward weights, stop_training, rationale, created_at)
    - Test: meta_decisions table created with all columns (run_id, algo, env, decision_type, decision_json, rationale, created_at)
    - Test: reward_adjustments table created with all columns (run_id, epoch_trigger, epoch_outcome, algo, env, weight_before, weight_after, outcome_sharpe, created_at)
    - Test: DDL is idempotent (CREATE TABLE IF NOT EXISTS)
    - Test: ALTER TABLE training_runs adds run_type and meta_rationale columns idempotently
    - Test: validate_memory.py checks for new tables
  </behavior>
  <action>
**src/swingrl/features/schema.py:**
Add three new DDL constants per RESEARCH.md Code Examples section:

_TRAINING_EPOCHS_DDL: CREATE TABLE IF NOT EXISTS training_epochs with columns:
- id INTEGER PRIMARY KEY
- run_id VARCHAR NOT NULL
- epoch INTEGER NOT NULL
- algo VARCHAR NOT NULL
- env VARCHAR NOT NULL
- sharpe DOUBLE, mdd DOUBLE
- reward_weight_profit DOUBLE, reward_weight_sharpe DOUBLE, reward_weight_drawdown DOUBLE, reward_weight_turnover DOUBLE
- stop_training BOOLEAN DEFAULT FALSE
- rationale TEXT
- created_at TIMESTAMP DEFAULT NOW()

_META_DECISIONS_DDL: CREATE TABLE IF NOT EXISTS meta_decisions with columns:
- id INTEGER PRIMARY KEY
- run_id VARCHAR NOT NULL
- algo VARCHAR NOT NULL, env VARCHAR NOT NULL
- decision_type VARCHAR NOT NULL (enum: 'run_config' | 'epoch_advice')
- decision_json TEXT NOT NULL
- rationale TEXT
- created_at TIMESTAMP DEFAULT NOW()

_REWARD_ADJUSTMENTS_DDL: CREATE TABLE IF NOT EXISTS reward_adjustments with columns:
- id INTEGER PRIMARY KEY
- run_id VARCHAR NOT NULL
- epoch_trigger INTEGER, epoch_outcome INTEGER
- algo VARCHAR NOT NULL, env VARCHAR NOT NULL
- weight_before TEXT (JSON), weight_after TEXT (JSON)
- outcome_sharpe DOUBLE
- created_at TIMESTAMP DEFAULT NOW()

_ALTER_TRAINING_RUNS_DDL: Two idempotent ALTER TABLE statements:
- ALTER TABLE training_runs ADD COLUMN IF NOT EXISTS run_type VARCHAR DEFAULT 'completed'
- ALTER TABLE training_runs ADD COLUMN IF NOT EXISTS meta_rationale TEXT

Add an apply_training_telemetry_ddl(conn) function that executes all four DDL blocks on a DuckDB connection. This function is called from train_pipeline.py before training starts.

**scripts/validate_memory.py:**
Update existing validation to also check for the three new tables (training_epochs, meta_decisions, reward_adjustments) and the two new columns on training_runs (run_type, meta_rationale). Use the same exit-0/exit-1 pattern already established.

**tests/test_memory_service.py (or tests/training/test_schema_migrations.py):**
Add test class TestTrainingTelemetryDDL:
- Test apply_training_telemetry_ddl creates all 3 tables on fresh DuckDB
- Test DDL is idempotent (run twice, no error)
- Test ALTER adds columns to existing training_runs table
- Test validate_memory.py integration with new tables
  </action>
  <verify>
    <automated>cd /Users/varunpanchal/Documents/Projects/Simplementix/SwingRL && uv run pytest tests/ -k "telemetry or migration" -v -x 2>&1 | tail -15</automated>
  </verify>
  <done>DuckDB has training_epochs, meta_decisions, reward_adjustments tables and training_runs has run_type + meta_rationale columns; all DDL is idempotent; validate_memory.py checks for new tables</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/memory/test_client.py tests/ -k "telemetry or migration or client" -v` all pass
- `uv run python -c "from swingrl.config.schema import load_config; c = load_config('config/swingrl.yaml'); print(c.memory_agent.api_key, c.training.sac_buffer_size if hasattr(c.training, 'sac_buffer_size') else 'N/A')"` prints defaults
- Full test suite still passes: `uv run pytest tests/ -v`
</verification>

<success_criteria>
- MemoryClient sends X-API-Key header when api_key is configured
- DuckDB training telemetry tables can be created idempotently
- SAC buffer_size is configurable via SwingRLConfig
- All existing tests continue to pass (backward compatible changes)
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-memory-agent-infrastructure-and-training/19.1-02-SUMMARY.md`
</output>
