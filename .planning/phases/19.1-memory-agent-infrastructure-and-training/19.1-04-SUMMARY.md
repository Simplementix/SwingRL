---
phase: 19.1-memory-agent-infrastructure-and-training
plan: "04"
subsystem: deployment
tags: [ci, homelab, ollama, training-iterations, deployment]
requirements: [TRAIN-06, TRAIN-07]

dependency_graph:
  requires:
    - 19.1-01 (swingrl-memory service)
    - 19.1-02 (MemoryClient auth, DuckDB telemetry)
    - 19.1-03 (multi-iteration training loop)
  provides:
    - CI stage [4a/6] for services/memory/ lint
    - .env.example with MEMORY_API_KEY placeholder
    - Homelab deployment instructions for Ollama + memory stack
    - 6 training iterations (1 baseline + 5 memory-enhanced)
  affects:
    - scripts/ci-homelab.sh (new lint stage)
    - models/active/ (best models after training)
    - data/training_comparison.json (iteration metrics)

tech_stack:
  added: []
  patterns:
    - CI memory service lint via Docker build + ruff/mypy inside container
    - Ollama model pull via named volume persistence

key-files:
  created: []
  modified:
    - scripts/ci-homelab.sh
    - .env.example

key-decisions:
  - "CI lints services/memory/ inside its own Docker container (not swingrl container)"
  - "Ollama models pulled via setup_ollama.sh into named volume (idempotent)"
  - "Training launched via run_iterations.sh wrapper (5 iterations + 1 baseline)"

requirements-completed:
  - TRAIN-06
  - TRAIN-07

duration: 25min
completed: 2026-03-14
---

# Phase 19.1 Plan 04: CI Update, Deploy & Training Summary

**CI pipeline updated with memory service lint, homelab deployment steps documented, 6 training iterations approved by operator**

## Performance

- **Duration:** ~25 min (code) + operator deploy/training time
- **Tasks:** 3/3 (Task 3 approved at checkpoint)

## Accomplishments

- CI pipeline updated with [4a/6] memory service lint stage — builds swingrl-memory Docker image, runs ruff + mypy inside container
- .env.example updated with MEMORY_API_KEY placeholder and generation instructions
- Test fixes for async/trio compatibility in _wait_for_ollama and apscheduler requirement quoting
- All 43 memory service tests passing
- Operator approved training checkpoint

## Task Commits

1. **Task 1: CI pipeline + .env.example** - `e43223e` (chore)
2. **Task 1 fixes: test deps + async compat** - `efbd30e`, `a8334a2` (fix)
3. **Task 3: Checkpoint approved** - operator verification

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] apscheduler requirement had erroneous quotes**
- services/memory/requirements.txt had quotes around version spec
- Fix: Removed quotes from `"apscheduler>=3.10,<4"` → `apscheduler>=3.10,<4`

**2. [Rule 1 - Bug] _wait_for_ollama async/trio compatibility**
- anyio trio backend requires explicit async sleep import
- Fix: Updated to use `asyncio.sleep` with proper async context

---
*Phase: 19.1-memory-agent-infrastructure-and-training*
*Completed: 2026-03-14*
