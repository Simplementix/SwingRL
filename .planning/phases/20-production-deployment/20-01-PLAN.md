---
phase: 20-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/swingrl/config/schema.py
  - config/swingrl.yaml
  - config/swingrl.prod.yaml.example
  - scripts/startup_checks.py
  - scripts/healthcheck.py
  - .env.example
  - tests/test_startup_checks.py
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "Production Docker build succeeds with all dependencies resolved (no ImportError)"
    - "Startup validation hard-fails on missing broker creds or unwritable bind mounts"
    - "Startup validation warns (but continues) on missing models, empty memory, unreachable memory service"
    - "Memory agent behavior is configurable per-endpoint via YAML (live_endpoints, live_bounds, macro_watcher)"
    - "Healthcheck probes memory service and Ollama reachability"
  artifacts:
    - path: "pyproject.toml"
      provides: "Production dependencies (apscheduler, sqlalchemy, requests, python-binance)"
      contains: "sqlalchemy"
    - path: "scripts/startup_checks.py"
      provides: "Pre-scheduler validation with hard-fail and warn modes"
      exports: ["run_startup_checks"]
    - path: "src/swingrl/config/schema.py"
      provides: "MemoryLiveBoundsConfig, MacroWatcherConfig, updated MemoryAgentConfig"
      contains: "live_bounds"
    - path: "tests/test_startup_checks.py"
      provides: "Tests for all startup check paths"
      min_lines: 80
  key_links:
    - from: "scripts/startup_checks.py"
      to: "scripts/main.py"
      via: "called by build_app() before scheduler init"
      pattern: "startup_checks"
    - from: "src/swingrl/config/schema.py"
      to: "config/swingrl.yaml"
      via: "Pydantic model loads YAML with new sections"
      pattern: "live_bounds"
---

<objective>
Fix production dependencies, extend config schema for memory live enrichment, create startup validation script, and update healthcheck — the foundation that all subsequent plans build on.

Purpose: Without correct dependencies the Docker image won't start. Without config schema extensions, memory live endpoints can't be configured. Without startup validation, deployment failures are silent.

Output: pyproject.toml with production deps, config schema with live_endpoints/live_bounds/macro_watcher, startup_checks.py, updated healthcheck.py, .env.example with all required keys.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-production-deployment/20-CONTEXT.md
@.planning/phases/20-production-deployment/20-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/swingrl/config/schema.py (existing):
```python
class MemoryLiveEndpointsConfig(BaseModel):
    obs_enrichment: bool = False
    blend_weights: bool = False
    position_advice: bool = False
    trade_veto: bool = False
    cycle_gate: bool = False
    risk_thresholds: bool = False

class MemoryAgentConfig(BaseModel):
    enabled: bool = False
    base_url: str = "http://swingrl-memory:8889"
    timeout_sec: float = 3.0
    # ... other fields ...
    api_key: str = ""
    live_endpoints: MemoryLiveEndpointsConfig = Field(default_factory=MemoryLiveEndpointsConfig)
```

From scripts/main.py (existing):
```python
def build_app(config_path: str = "config/swingrl.yaml") -> dict[str, Any]:
    # ... config loading, DB init, pipeline creation ...
    # startup_checks should be called HERE, before scheduler creation
```

From scripts/healthcheck.py (existing):
```python
def check_sqlite(db_path: Path) -> None: ...
def check_duckdb(db_path: Path) -> None: ...
def main() -> int:  # Returns 0 healthy, 1 unhealthy
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix production dependencies and extend config schema</name>
  <files>pyproject.toml, src/swingrl/config/schema.py, config/swingrl.yaml, config/swingrl.prod.yaml.example, .env.example</files>
  <behavior>
    - Test: MemoryLiveBoundsConfig has blend_weight_delta=0.20, max_dd_tighten_min=0.02, daily_limit_tighten_min=0.005 defaults
    - Test: MacroWatcherConfig has enabled=True, poll_interval_seconds=300, lookforward_hours=24, source="finnhub", high_impact_only=True defaults
    - Test: MemoryAgentConfig.live_bounds and .macro_watcher are accessible on SwingRLConfig
    - Test: load_config() with live_bounds YAML section correctly populates config
    - Test: Env var override SWINGRL_MEMORY_AGENT__LIVE_BOUNDS__BLEND_WEIGHT_DELTA=0.10 works
  </behavior>
  <action>
    1. Move `apscheduler>=3.10,<4` from `[dependency-groups.dev]` to `[project.dependencies]` in pyproject.toml. Add `sqlalchemy>=2.0,<3`, `requests>=2.28`, `python-binance>=1.0` to `[project.dependencies]`. Keep apscheduler in dev group too (tests need it). Add `binance` to mypy ignore_missing_imports list.

    2. Add new Pydantic models in schema.py (BEFORE MemoryAgentConfig):
       - `MemoryLiveBoundsConfig(BaseModel)`: blend_weight_delta (float, default 0.20), max_dd_tighten_min (float, default 0.02), daily_limit_tighten_min (float, default 0.005)
       - `MacroWatcherConfig(BaseModel)`: enabled (bool, default True), poll_interval_seconds (int, default 300), lookforward_hours (int, default 24), source (Literal["finnhub", "hardcoded"], default "finnhub"), high_impact_only (bool, default True)

    3. Add to MemoryAgentConfig: `live_bounds: MemoryLiveBoundsConfig = Field(default_factory=MemoryLiveBoundsConfig)`, `macro_watcher: MacroWatcherConfig = Field(default_factory=MacroWatcherConfig)`. Note: live_endpoints field already exists.

    4. Update config/swingrl.yaml: add memory_agent.live_endpoints (all true), live_bounds, and macro_watcher sections per RESEARCH.md examples.

    5. Update config/swingrl.prod.yaml.example with same sections plus comments.

    6. Update .env.example: add `SWINGRL_ALERTING__ALERTS_WEBHOOK_URL=""`, `SWINGRL_ALERTING__DAILY_WEBHOOK_URL=""` (per Pitfall 4 from RESEARCH.md). Keep existing keys.
  </action>
  <verify>
    <automated>uv run pytest tests/config/ -x -v -k "test_" && uv run python -c "from swingrl.config.schema import load_config; c = load_config('config/swingrl.yaml'); print(c.memory_agent.live_bounds.blend_weight_delta, c.memory_agent.macro_watcher.poll_interval_seconds)"</automated>
  </verify>
  <done>pyproject.toml has 4 new production deps, config schema has MemoryLiveBoundsConfig + MacroWatcherConfig, swingrl.yaml has live sections, .env.example has all required keys</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create startup validation script and extend healthcheck</name>
  <files>scripts/startup_checks.py, scripts/healthcheck.py, tests/test_startup_checks.py</files>
  <behavior>
    - Test: run_startup_checks hard-fails (sys.exit(1)) when Alpaca credentials are missing/empty
    - Test: run_startup_checks hard-fails when bind mount dirs are not writable
    - Test: run_startup_checks hard-fails when required env keys are missing
    - Test: run_startup_checks warns (returns True) when model files are missing
    - Test: run_startup_checks warns when memory service is unreachable
    - Test: run_startup_checks warns when memory.db has zero rows
    - Test: run_startup_checks passes all checks when everything is properly configured (mocked)
    - Test: healthcheck returns 0 when memory + Ollama are reachable, 1 when not (warn only)
  </behavior>
  <action>
    1. Create `scripts/startup_checks.py` with two check categories:

    HARD_FAIL_CHECKS (sys.exit(1) on failure):
    - `check_env_keys()`: verify ALPACA_API_KEY, ALPACA_SECRET_KEY, BINANCE_API_KEY, BINANCE_SECRET_KEY all present and non-empty in os.environ. Check SWINGRL_MEMORY_AGENT__API_KEY if memory_agent.enabled.
    - `check_broker_credentials()`: ping Alpaca paper API via `TradingClient(paper=True).get_account()`. Ping Binance.US API via `from binance.client import Client; Client(api_key, api_secret, tld='us').get_account()`. Import lazily; catch all exceptions.
    - `check_db_tables()`: verify SQLite trading_ops.db has expected tables (positions, trades, etc.). Verify DuckDB market_data.ddb responds to SELECT 1. Create tables if absent using existing DatabaseManager.ensure_tables().
    - `check_bind_mount_writability()`: for each dir in [data/, db/, models/, logs/, status/], touch a temp file and delete it. Hard fail if any write fails.

    WARN_CHECKS (log.warning, continue):
    - `check_model_files()`: check models/active/{equity,crypto}/{ppo,a2c,sac}/model.zip exists. Warn per missing file.
    - `check_memory_service()`: HTTP GET to swingrl-memory:8889/health with 3s timeout (stdlib urllib). Warn on any error.
    - `check_memory_seeding()`: open db/memory.db with sqlite3, count rows in memories table. Warn if 0 with prominent message "MEMORY NOT SEEDED -- run seed_memory_from_backtest.py before first live cycle".

    Top-level `run_startup_checks(config: SwingRLConfig | None = None) -> bool` function. Also runnable standalone: `if __name__ == "__main__": run_startup_checks()`.

    2. Extend `scripts/healthcheck.py`: add `check_memory_service()` and `check_ollama()` functions. Both use stdlib urllib with 3s timeout. On failure, print "DEGRADED: memory/ollama unreachable" but still return 0 (fail-open — trading continues). Only return 1 for DB check failures (existing behavior).

    3. Write comprehensive tests in `tests/test_startup_checks.py` mocking all external calls (broker APIs, filesystem, HTTP). Use `pytest.MonkeyPatch` for env vars, `unittest.mock.patch` for API calls.
  </action>
  <verify>
    <automated>uv run pytest tests/test_startup_checks.py -x -v</automated>
  </verify>
  <done>startup_checks.py exits 1 on hard failures, warns on soft failures. healthcheck.py reports degraded status for memory/Ollama but doesn't fail. All paths tested with mocks.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/config/ tests/test_startup_checks.py -x -v` passes
- `uv run python -c "from swingrl.config.schema import load_config; c = load_config('config/swingrl.yaml'); assert c.memory_agent.live_bounds.blend_weight_delta == 0.20"` succeeds
- `uv run python -c "import scripts.startup_checks"` imports without error
</verification>

<success_criteria>
- pyproject.toml has apscheduler + sqlalchemy + requests + python-binance in [project.dependencies]
- Config schema has MemoryLiveBoundsConfig, MacroWatcherConfig on MemoryAgentConfig
- startup_checks.py has 4 hard-fail + 3 warn checks with structlog output
- healthcheck.py includes memory + Ollama reachability (fail-open)
- .env.example has all required keys including Discord webhooks
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-production-deployment/20-01-SUMMARY.md`
</output>
