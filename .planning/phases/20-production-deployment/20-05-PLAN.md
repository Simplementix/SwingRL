---
phase: 20-production-deployment
plan: 05
type: execute
wave: 4
depends_on: ["20-03", "20-04"]
files_modified:
  - scripts/ci-homelab.sh
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04, DEPLOY-05]

must_haves:
  truths:
    - "CI pipeline passes with all new code (startup_checks, live client, fill ingestors, macro watcher, failure tracker)"
    - "Docker compose build succeeds for both containers on homelab x86"
    - "Homelab deployment runs deploy.sh end-to-end with all containers healthy"
    - "Memory is seeded from backtest data via seed_memory_from_backtest.py"
    - "Smoke test passes: real paper equity cycle (Alpaca paper orders) + crypto cycle complete without error"
    - "show_schedule.py displays correct next fire times in ET and UTC"
  artifacts:
    - path: "scripts/ci-homelab.sh"
      provides: "Updated CI with memory service live endpoint tests"
      contains: "memory"
  key_links:
    - from: "scripts/ci-homelab.sh"
      to: "services/memory/"
      via: "CI tests both swingrl and memory service code"
      pattern: "memory"
---

<objective>
Update CI pipeline, run homelab CI, deploy to homelab, seed memory, and verify end-to-end paper trading via smoke test. This is the deployment checkpoint that confirms everything works on the production hardware.

Purpose: All code is written -- this plan validates it runs correctly on the homelab x86 Docker environment with real broker credentials, real models, and real memory service.

Output: Passing CI, healthy Docker stack on homelab, seeded memory, successful smoke test.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-production-deployment/20-CONTEXT.md
@.planning/phases/20-production-deployment/20-RESEARCH.md
@.planning/phases/20-production-deployment/20-01-SUMMARY.md
@.planning/phases/20-production-deployment/20-02-SUMMARY.md
@.planning/phases/20-production-deployment/20-03-SUMMARY.md
@.planning/phases/20-production-deployment/20-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI pipeline and run homelab CI</name>
  <files>scripts/ci-homelab.sh</files>
  <action>
    1. Update `scripts/ci-homelab.sh`:
       - Add memory service lint stage: `cd services/memory && ruff check . && cd ../..` (if not already present from Phase 19.1)
       - Add memory service test stage: `PYTHONPATH=services/memory:src uv run pytest tests/memory_service/ -x -v`
       - Add production Docker compose build validation: `docker compose -f docker-compose.prod.yml build` (build only, no start)
       - Ensure all new test files are included in the test run

    2. Push branch to origin and run homelab CI:
       ```bash
       git push origin gsd/phase-20-production-deployment
       ssh homelab "cd ~/swingrl && git fetch origin && git checkout gsd/phase-20-production-deployment && git pull origin gsd/phase-20-production-deployment && bash scripts/ci-homelab.sh --no-cache"
       ```

    3. Fix any CI failures before proceeding (import errors, missing deps in Docker, test failures on x86).
  </action>
  <verify>
    <automated>ssh homelab "cd ~/swingrl && bash scripts/ci-homelab.sh --no-cache"</automated>
  </verify>
  <done>CI passes on homelab x86 with all new tests, Docker compose build succeeds for both containers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to homelab and verify end-to-end paper trading</name>
  <action>
    Deploy the full Docker stack to homelab, seed memory from backtest data, and run smoke test to verify end-to-end paper trading works. This is the final deployment verification before Phase 20 is complete.
  </action>
  <what-built>
    Complete Phase 20 production deployment:
    - Production dependencies fixed (apscheduler, sqlalchemy, requests, python-binance)
    - Config schema extended with live_endpoints, live_bounds, macro_watcher
    - Startup validation (7 checks: 4 hard-fail + 3 warn)
    - Memory live endpoints (5 endpoints in swingrl-memory service)
    - LiveMemoryClient with fail-open semantics and consecutive latency alerting (3 slow calls -> Discord + structlog)
    - execute_cycle() memory hooks (cycle_gate, blend_weights, risk_thresholds, position_advice, trade_veto)
    - ConsecutiveFailureTracker (3 failures per env -> Discord warning + structlog error)
    - CircuitBreaker save/restore override pattern
    - AlpacaFillIngestor + BinanceFillIngestor daemon threads
    - MacroWatcher polling thread (Finnhub + hardcoded fallback)
    - show_schedule.py, seed_memory_from_backtest.py, deploy.sh, smoke_test.py (real paper mode)
    - Docker resource limits updated (swingrl 8GB/4CPU, memory 1GB/1CPU)
    - CI updated and passing on homelab
  </what-built>
  <how-to-verify>
    1. SSH to homelab: `ssh homelab`
    2. Run deploy.sh: `cd ~/swingrl && bash scripts/deploy.sh`
    3. Wait for all containers healthy: `docker compose -f docker-compose.prod.yml ps`
       - Expected: swingrl, swingrl-memory, swingrl-ollama, swingrl-dashboard all "healthy" or "running"
    4. Seed memory: `docker exec swingrl python scripts/seed_memory_from_backtest.py`
       - Expected: prints ingested run count, calls /consolidate
    5. Show schedule: `docker exec swingrl python scripts/show_schedule.py`
       - Expected: table with 12+ jobs, next fire times in both ET and UTC
       - Verify equity_cycle shows ~4:15 PM ET
       - Verify crypto_cycle shows next 4H boundary in UTC
    6. Run smoke test (real paper mode): `docker exec swingrl python scripts/smoke_test.py --config config/swingrl.yaml`
       - Expected: pass/fail checklist, exit 0
       - Key checks: features assembled, model loaded, memory endpoints called, real Alpaca paper order submitted (equity), simulated fill (crypto)
       - Verify Alpaca paper dashboard shows the test order
    7. Check logs: `docker compose -f docker-compose.prod.yml logs swingrl --tail 50`
       - Expected: startup_checks passed, scheduler_started, no errors
    8. Check memory service: `curl -H "X-API-Key: $(grep MEMORY_API_KEY ~/swingrl/.env | cut -d= -f2)" http://localhost:8889/health`
       - Expected: 200 OK with health status
  </how-to-verify>
  <resume-signal>Type "approved" to confirm deployment is healthy, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- CI passes on homelab
- All 4 containers healthy on homelab
- Memory seeded from backtest data
- Smoke test exits 0 with real Alpaca paper order submitted
- show_schedule.py displays correct fire times
- No errors in container logs after startup
</verification>

<success_criteria>
- Homelab Docker stack running with all containers healthy
- Startup validation passed (structlog output confirms all checks)
- Memory service reachable with seeded memories
- APScheduler jobs registered with correct timezones
- Smoke test confirms real paper trading cycle works for equity (Alpaca paper orders) and crypto (simulated fills)
- Paper trading ready to fire on next scheduled trigger
</success_criteria>

<output>
After completion, create `.planning/phases/20-production-deployment/20-05-SUMMARY.md`
</output>
