---
phase: 20-production-deployment
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - src/swingrl/memory/live/__init__.py
  - src/swingrl/memory/live/client.py
  - src/swingrl/memory/live/fill_ingestor.py
  - src/swingrl/execution/pipeline.py
  - src/swingrl/execution/risk/circuit_breaker.py
  - src/swingrl/training/ensemble.py
  - src/swingrl/scheduler/failure_tracker.py
  - tests/memory/test_live_client.py
  - tests/execution/test_fill_ingestor.py
  - tests/execution/test_pipeline_memory.py
  - tests/execution/test_circuit_breaker_overrides.py
  - tests/scheduler/test_failure_tracker.py
autonomous: true
requirements: [DEPLOY-04, DEPLOY-05]

must_haves:
  truths:
    - "LiveMemoryClient calls all 5 endpoints with fail-open semantics (returns safe default on any error)"
    - "LiveMemoryClient tracks consecutive slow calls (>3s) and fires structlog warning + Discord alert after 3 consecutive"
    - "execute_cycle() calls cycle_gate before proceeding and returns [] if gate rejects"
    - "execute_cycle() applies blend weight overrides clamped to +/-0.20 from baseline"
    - "execute_cycle() applies circuit breaker risk threshold overrides (tighten only)"
    - "execute_cycle() checks trade_veto per signal before broker submission"
    - "CircuitBreaker has apply_overrides() save+restore pattern"
    - "AlpacaFillIngestor subscribes to trade_updates stream as daemon thread"
    - "BinanceFillIngestor is no-op in paper mode"
    - "Consecutive failures per env are tracked in SQLite; after 3 consecutive failures a Discord warning + structlog error fires"
  artifacts:
    - path: "src/swingrl/memory/live/client.py"
      provides: "LiveMemoryClient with 5 fail-open endpoint methods and consecutive latency alerting"
      exports: ["LiveMemoryClient"]
      min_lines: 100
    - path: "src/swingrl/memory/live/fill_ingestor.py"
      provides: "AlpacaFillIngestor and BinanceFillIngestor daemon threads"
      exports: ["AlpacaFillIngestor", "BinanceFillIngestor"]
      min_lines: 80
    - path: "src/swingrl/execution/pipeline.py"
      provides: "execute_cycle() with 5 memory hook integration points and consecutive failure tracking"
      contains: "cycle_gate"
    - path: "src/swingrl/execution/risk/circuit_breaker.py"
      provides: "apply_overrides() and reset_overrides() methods"
      contains: "apply_overrides"
    - path: "src/swingrl/scheduler/failure_tracker.py"
      provides: "ConsecutiveFailureTracker using SQLite for per-env failure counting"
      exports: ["ConsecutiveFailureTracker"]
      min_lines: 40
  key_links:
    - from: "src/swingrl/memory/live/client.py"
      to: "services/memory/routers/live.py"
      via: "HTTP POST to /live/* endpoints via stdlib urllib"
      pattern: "urllib\\.request"
    - from: "src/swingrl/execution/pipeline.py"
      to: "src/swingrl/memory/live/client.py"
      via: "LiveMemoryClient calls within execute_cycle()"
      pattern: "LiveMemoryClient"
    - from: "src/swingrl/execution/pipeline.py"
      to: "src/swingrl/execution/risk/circuit_breaker.py"
      via: "apply_overrides() called after risk_thresholds endpoint"
      pattern: "apply_overrides"
    - from: "src/swingrl/execution/pipeline.py"
      to: "src/swingrl/scheduler/failure_tracker.py"
      via: "record_success/record_failure called after execute_cycle completes/fails"
      pattern: "failure_tracker"
---

<objective>
Build the LiveMemoryClient, FillIngestor daemon threads, consecutive failure tracker, and integrate all 5 memory hooks into ExecutionPipeline.execute_cycle(). This is the core integration that makes the memory agent control the trading pipeline.

Purpose: The memory agent must influence every trading cycle through gate/weights/thresholds/advice/veto decisions, fill events must be ingested back into memory for learning, and consecutive failures must be tracked for operational alerting.

Output: LiveMemoryClient with fail-open methods and latency alerting, FillIngestors for Alpaca and Binance, execute_cycle() with memory hooks, CircuitBreaker override pattern, ConsecutiveFailureTracker.
</objective>

<execution_context>
@/Users/varunpanchal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/varunpanchal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-production-deployment/20-CONTEXT.md
@.planning/phases/20-production-deployment/20-RESEARCH.md
@.planning/phases/20-production-deployment/20-01-SUMMARY.md
@.planning/phases/20-production-deployment/20-02-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 (config schema) and Plan 02 (live endpoints). -->

From src/swingrl/config/schema.py (after Plan 01):
```python
class MemoryLiveBoundsConfig(BaseModel):
    blend_weight_delta: float = 0.20
    max_dd_tighten_min: float = 0.02
    daily_limit_tighten_min: float = 0.005

class MemoryAgentConfig(BaseModel):
    enabled: bool = False
    base_url: str = "http://swingrl-memory:8889"
    timeout_sec: float = 3.0
    api_key: str = ""
    live_endpoints: MemoryLiveEndpointsConfig
    live_bounds: MemoryLiveBoundsConfig
    macro_watcher: MacroWatcherConfig
```

From services/memory/routers/live.py (after Plan 02):
```python
# POST /live/cycle_gate {env, context} -> {approved, rationale, latency_ms}
# POST /live/blend_weights {env, current_weights, context} -> {weights, rationale, latency_ms}
# POST /live/risk_thresholds {env, context} -> {max_dd, daily_limit, rationale, latency_ms}
# POST /live/position_advice {env, signals, context} -> {advice, rationale, latency_ms}
# POST /live/trade_veto {env, signal, context} -> {vetoed, rationale, latency_ms}
```

From src/swingrl/execution/pipeline.py (existing):
```python
class ExecutionPipeline:
    def execute_cycle(self, env_name: str, dry_run: bool = False) -> list[FillResult]:
        # Step 1: Check CB state
        # Step 2: Check turbulence
        # Step 3: Get observation
        # Step 7: Get ensemble weights + blend
        # Step 9: Signal interpretation
        # Step 10: Per-signal processing (size -> validate -> submit -> fill)
```

From src/swingrl/execution/risk/circuit_breaker.py (existing):
```python
class CircuitBreaker:
    def __init__(self, environment, db, config):
        self._max_dd = config.equity.max_drawdown_pct  # or crypto
        self._daily_limit = config.equity.daily_loss_limit_pct
```

From src/swingrl/memory/client.py (pattern for LiveMemoryClient):
```python
class MemoryClient:
    def __init__(self, base_url, default_timeout, api_key): ...
    # Uses stdlib urllib, fail-open, returns False on error
```

From src/swingrl/alerting/ (existing Alerter for Discord):
```python
class Alerter:
    def send_alert(self, message: str, level: str = "warning") -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LiveMemoryClient with latency alerting, CircuitBreaker overrides, ConsecutiveFailureTracker, and integrate into execute_cycle()</name>
  <files>src/swingrl/memory/live/__init__.py, src/swingrl/memory/live/client.py, src/swingrl/execution/risk/circuit_breaker.py, src/swingrl/execution/pipeline.py, src/swingrl/scheduler/failure_tracker.py, tests/memory/test_live_client.py, tests/execution/test_circuit_breaker_overrides.py, tests/execution/test_pipeline_memory.py, tests/scheduler/test_failure_tracker.py</files>
  <behavior>
    - Test: LiveMemoryClient.query_cycle_gate returns True (allow) on connection refused
    - Test: LiveMemoryClient.query_cycle_gate returns True (allow) on timeout
    - Test: LiveMemoryClient.query_cycle_gate returns True (allow) on HTTP 500
    - Test: LiveMemoryClient.query_cycle_gate returns True (allow) on malformed JSON
    - Test: LiveMemoryClient.query_blend_weights returns {} on any error
    - Test: LiveMemoryClient.query_trade_veto returns False (no veto) on any error
    - Test: LiveMemoryClient.query_risk_thresholds returns (None, None) on any error
    - Test: LiveMemoryClient fires structlog warning after 3 consecutive calls with latency >3s
    - Test: LiveMemoryClient resets consecutive slow counter when a fast call occurs
    - Test: CircuitBreaker.apply_overrides saves originals and tightens only (min of old and new)
    - Test: CircuitBreaker.apply_overrides does NOT widen thresholds
    - Test: CircuitBreaker.reset_overrides restores original values
    - Test: ConsecutiveFailureTracker.record_failure increments per-env counter in SQLite
    - Test: ConsecutiveFailureTracker.record_success resets counter to 0
    - Test: ConsecutiveFailureTracker fires Discord alert + structlog error at 3 consecutive failures
    - Test: ConsecutiveFailureTracker does not alert again until counter resets and hits 3 again
    - Test: execute_cycle returns [] when cycle_gate rejects (memory_agent.enabled + cycle_gate enabled)
    - Test: execute_cycle skips signal when trade_veto returns vetoed=True
    - Test: execute_cycle applies blend weight clamped to +-0.20 from baseline
    - Test: execute_cycle calls reset_overrides at start and apply_overrides after risk_thresholds
    - Test: execute_cycle calls failure_tracker.record_success on normal completion
    - Test: execute_cycle calls failure_tracker.record_failure on exception
  </behavior>
  <action>
    1. Create `src/swingrl/memory/live/__init__.py` (empty).

    2. Create `src/swingrl/memory/live/client.py` -- `LiveMemoryClient` class following MemoryClient pattern:
       - Constructor: base_url, timeout (from config.memory_agent.timeout_sec), api_key, alerter (optional Alerter instance for Discord alerts)
       - Internal state: `_consecutive_slow_calls: int = 0` (counter for latency alerting)
       - `query_cycle_gate(env, context) -> bool`: POST /live/cycle_gate. Return True on any error (fail-open: allow cycle).
       - `query_blend_weights(env, current_weights, context) -> dict[str, float]`: POST /live/blend_weights. Return {} on error (no adjustment).
       - `query_risk_thresholds(env, context) -> tuple[float | None, float | None]`: POST /live/risk_thresholds. Return (None, None) on error.
       - `query_position_advice(env, signals, context) -> str`: POST /live/position_advice. Return "" on error.
       - `query_trade_veto(env, signal, context) -> bool`: POST /live/trade_veto. Return False on error (no veto).
       - `ingest_post_trade(env, fills, context) -> None`: POST /ingest (existing endpoint). Fire-and-forget via background queue.
       - All methods use stdlib urllib.request.Request + urlopen with timeout, nosec B310, structlog logging.
       - Log every call with: endpoint, env, decision, rationale, latency_ms, clamped (bool).
       - **Latency alerting**: After each successful call, check if latency_ms > 3000. If yes, increment `_consecutive_slow_calls`. If counter reaches 3: fire `log.warning("memory_latency_alert", consecutive_slow_calls=3)` and call `self._alerter.send_alert("Memory service latency: 3 consecutive calls >3s", level="warning")` if alerter is set. If latency_ms <= 3000, reset counter to 0.

    3. Add to `CircuitBreaker` in circuit_breaker.py:
       - `apply_overrides(self, max_dd: float | None, daily_limit: float | None) -> None`: Save `_saved_max_dd = self._max_dd`, `_saved_daily_limit = self._daily_limit`. Apply: `self._max_dd = min(self._max_dd, max_dd)` (tighten only). Log original vs new.
       - `reset_overrides(self) -> None`: Restore from `_saved_*` fields if they exist. Log.

    4. Create `src/swingrl/scheduler/failure_tracker.py` -- `ConsecutiveFailureTracker` class:
       - Constructor: db (DatabaseManager or sqlite3 connection), alerter (optional Alerter), threshold (int, default 3)
       - On init, create SQLite table `consecutive_failures` if not exists: env TEXT PRIMARY KEY, count INTEGER DEFAULT 0, last_failure_time TEXT
       - `record_failure(env: str) -> None`: Increment count for env in SQLite. If count reaches threshold: fire `log.error("consecutive_failure_alert", env=env, count=threshold)` and call `self._alerter.send_alert(f"ALERT: {threshold} consecutive failures for {env}", level="error")` if alerter set. Only alert on the exact threshold crossing (not every subsequent failure).
       - `record_success(env: str) -> None`: Reset count to 0 for env in SQLite. Log at debug level.
       - `get_count(env: str) -> int`: Read current count (for testing/observability).

    5. Modify `ExecutionPipeline.__init__()`: if `config.memory_agent.enabled`, create `LiveMemoryClient(base_url=config.memory_agent.base_url, timeout=config.memory_agent.timeout_sec, api_key=config.memory_agent.api_key, alerter=self._alerter)`. Store as `self._live_client` (None if disabled). Also create `ConsecutiveFailureTracker(db=self._db, alerter=self._alerter)` and store as `self._failure_tracker`. Create a background ingest queue: `self._ingest_queue = queue.Queue(maxsize=100)` and start a daemon worker thread that drains the queue and calls `MemoryClient.ingest()`.

    6. Modify `execute_cycle()` with memory hooks (all gated by `self._live_client is not None` AND per-endpoint config toggle):
       - After Step 1 (CB check): call `reset_overrides()` on env CB.
       - After Step 1 (new Step 1.5): if `config.memory_agent.live_endpoints.cycle_gate`, call `self._live_client.query_cycle_gate(env_name, context)`. Build context dict with HMM regime, recent P&L, portfolio value. If returns False, log and return [].
       - After turbulence check (new Step 2.5): if `config.memory_agent.live_endpoints.risk_thresholds`, call `self._live_client.query_risk_thresholds(env_name, context)`. Call `cb.apply_overrides(max_dd, daily_limit)`.
       - After Step 7 (ensemble weights): if `config.memory_agent.live_endpoints.blend_weights`, call `self._live_client.query_blend_weights(env_name, weights, context)`. Clamp each algo weight to +-delta from baseline. Re-normalize to sum=1.0.
       - After Step 9 (signals generated): if `config.memory_agent.live_endpoints.position_advice`, call `self._live_client.query_position_advice(env_name, signals_as_dicts, context)`. Log the advice (advisory only, no action).
       - Inside Step 10 loop, before submit: if `config.memory_agent.live_endpoints.trade_veto`, call `self._live_client.query_trade_veto(env_name, signal_as_dict, context)`. If vetoed, log and continue (skip signal).
       - After Step 11: enqueue post-trade ingest to background queue (non-blocking put_nowait, drop with log.warning if full).
       - **Wrap entire execute_cycle body in try/except**: on success call `self._failure_tracker.record_success(env_name)`, on exception call `self._failure_tracker.record_failure(env_name)` then re-raise.

    7. Write tests:
       - `tests/memory/test_live_client.py`: Test all 5 methods with mocked urllib responses (success, connection refused, timeout, 500, malformed JSON). Verify fail-open behavior. Test consecutive slow call alerting (3 consecutive >3s fires warning, fast call resets counter).
       - `tests/execution/test_circuit_breaker_overrides.py`: Test apply_overrides tightens only, reset_overrides restores, apply with None values is no-op.
       - `tests/execution/test_pipeline_memory.py`: Test execute_cycle with mocked LiveMemoryClient. Verify cycle_gate blocks, trade_veto skips, blend weights clamped, overrides applied/reset. Verify failure_tracker.record_success called on normal completion, record_failure on exception.
       - `tests/scheduler/test_failure_tracker.py`: Test record_failure increments, record_success resets, alert fires at threshold=3, no re-alert until reset.
  </action>
  <verify>
    <automated>uv run pytest tests/memory/test_live_client.py tests/execution/test_circuit_breaker_overrides.py tests/execution/test_pipeline_memory.py tests/scheduler/test_failure_tracker.py -x -v</automated>
  </verify>
  <done>LiveMemoryClient calls all 5 endpoints fail-open with latency alerting. ConsecutiveFailureTracker fires Discord + structlog at 3 failures. execute_cycle() has all memory hooks gated by config toggles with failure tracking. CircuitBreaker has save/restore override pattern. All tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FillIngestor daemon threads for Alpaca and Binance</name>
  <files>src/swingrl/memory/live/fill_ingestor.py, tests/execution/test_fill_ingestor.py</files>
  <behavior>
    - Test: AlpacaFillIngestor._handle_trade_update enqueues fill events for "fill" and "partial_fill" event types
    - Test: AlpacaFillIngestor._handle_trade_update ignores non-fill events (e.g., "new", "canceled")
    - Test: AlpacaFillIngestor constructs FillMemory dict with symbol, side, qty, price, timestamp
    - Test: BinanceFillIngestor is no-op when not started (paper mode)
    - Test: BinanceFillIngestor._handle_message processes executionReport with FILLED status
    - Test: BinanceFillIngestor._handle_message ignores non-fill messages
    - Test: Background ingest worker drains queue and calls MemoryClient.ingest()
  </behavior>
  <action>
    1. Create `src/swingrl/memory/live/fill_ingestor.py`:

    **AlpacaFillIngestor:**
    - Constructor: api_key, secret_key, paper (bool), memory_client (MemoryClient), ingest_queue (queue.Queue)
    - Creates TradingStream from alpaca.trading.stream with `paper=paper`
    - `_handle_trade_update(data)`: async handler. Check `data.event` in ("fill", "partial_fill"). Extract symbol, side, qty, price, timestamp. Build dict and `self._ingest_queue.put_nowait(dict)`. Wrap entire handler in try/except (Pitfall: exceptions kill the stream).
    - `start()`: create daemon Thread targeting `self._stream.run`, start it. Log "alpaca_fill_ingestor_started".
    - Uses same ALPACA_API_KEY / ALPACA_SECRET_KEY as TradingClient (per RESEARCH.md Open Question 2).

    **BinanceFillIngestor:**
    - Constructor: api_key, secret_key, memory_client (MemoryClient), ingest_queue (queue.Queue)
    - Creates `ThreadedWebsocketManager(api_key=api_key, api_secret=secret_key, tld="us")` from `binance` package
    - `_handle_message(msg)`: Check `msg.get("e") == "executionReport"` and `msg.get("X") in ("FILLED", "PARTIALLY_FILLED")`. Extract symbol (s), side (S), qty (q), price (p). Build dict and enqueue.
    - `start()`: `self._bm.start()` then `self._bm.start_user_socket(callback=self._handle_message)`. Log.
    - Auto-reconnect is handled by ThreadedWebsocketManager internally (built-in 5-retry).
    - In paper mode, this class is never instantiated -- guard is in build_app().

    **BackgroundIngestWorker:**
    - Simple function `run_ingest_worker(ingest_queue, memory_client)` that runs in a daemon thread.
    - Loop: `item = queue.get(timeout=5)`, call `memory_client.ingest(item)`, `queue.task_done()`. Catch all exceptions.

    2. Write tests in `tests/execution/test_fill_ingestor.py`:
    - Mock `alpaca.trading.stream.TradingStream` -- test that subscribe_trade_updates callback processes fill events correctly.
    - Mock `binance.ThreadedWebsocketManager` -- test that start_user_socket callback processes executionReport.
    - Test background worker drains queue items.
    - Test handler exceptions are swallowed (don't kill stream).
  </action>
  <verify>
    <automated>uv run pytest tests/execution/test_fill_ingestor.py -x -v</automated>
  </verify>
  <done>AlpacaFillIngestor subscribes to trade_updates as daemon thread. BinanceFillIngestor processes executionReport events. Both enqueue to background ingest queue. Paper mode guard documented. All tested with mocked streams.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/memory/test_live_client.py tests/execution/test_circuit_breaker_overrides.py tests/execution/test_pipeline_memory.py tests/execution/test_fill_ingestor.py tests/scheduler/test_failure_tracker.py -x -v` passes
- `uv run python -c "from swingrl.memory.live.client import LiveMemoryClient; print('OK')"` succeeds
- `uv run python -c "from swingrl.scheduler.failure_tracker import ConsecutiveFailureTracker; print('OK')"` succeeds
</verification>

<success_criteria>
- LiveMemoryClient has 5 fail-open methods using stdlib urllib with consecutive latency alerting
- ConsecutiveFailureTracker tracks per-env failures in SQLite, alerts at 3 consecutive via Discord + structlog
- execute_cycle() has all 5 memory hooks at correct integration points with failure tracking
- CircuitBreaker has apply_overrides (tighten only) + reset_overrides
- AlpacaFillIngestor + BinanceFillIngestor daemon threads created
- Background ingest worker drains queue to MemoryClient.ingest()
- All paths tested (success, failure, timeout, malformed response, latency alert, consecutive failures)
</success_criteria>

<output>
After completion, create `.planning/phases/20-production-deployment/20-03-SUMMARY.md`
</output>
